## 设计写入消解系统 (Write Reconciliation)

### 参考
WorldDB — Ontology-aware write reconciliation for multi-agent memory

### 目标
多 Agent 并发写入同一实体/关系时自动消解冲突，类似 Git merge 但用于记忆。

### 当前状态
- 无冲突检测，后写覆盖先写（last-write-wins）
- 无事务隔离级别
- 已有 MemTX (两阶段提交) 和 MemClaw (supersession_of 链)

### 设计方案对比
- CRDT 无冲突数据类型 — 适合计数器/集合，不适合语义知识冲突
- **推荐**：本体感知三路合并 (Ontology-aware Merge) — 参考 WorldDB

### 三路合并策略
1. LWW (Last-Write-Wins) — 简单时间戳比较
2. Merge — 属性级合并（同名属性取最新，不同属性叠加）
3. Additive — 实体级叠加（不覆盖，保留所有版本）

### 需要读取的文件
1. /home/admin/shm/core/transaction_manager.py — 已有事务机制
2. /home/admin/shm/graph/ryu_store.py — 图存储
3. /home/admin/shm/api/_routes.py — 写入路由

### 设计要求
- 不改现有写入路径默认行为
- 新增 resolve_conflicts() 接口
- 冲突日志可查询
- 并发 10 agent × 100 writes 无数据丢失
