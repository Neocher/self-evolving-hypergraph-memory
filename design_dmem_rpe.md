# D-MEM 多巴胺门控借鉴设计（RPE 写入门控）

> 2026-08-24 ｜ 用户提供的 D-MEM 研究（模拟哺乳动物多巴胺 RPE 机制）
> 核心：RPE 路由 + 批判路由器 → 95% 信息过滤，仅关键事实深度记忆 → Token -80% + 抗噪

## 一、D-MEM 核心机制（研究摘要提炼）

| D-MEM 机制 | 生物学原型 | 效果 |
|---|---|---|
| RPE（奖励预测误差）路由 | 多巴胺神经元编码预测误差 | 惊奇度量化 |
| 批判路由器 | 前额叶评估长期效用 | 三分流（深度/缓存/忽略） |
| 深度记忆演化 | 海马体巩固 | 关键事实 5% 深度整合 |
| 快速缓存 | 工作记忆 | 中价值信息轻量保留 |
| 忽略 | 感觉遗忘 | 95% 噪声过滤 |

## 二、SHM 现状差距

| 维度 | SHM 现状 | D-MEM 借鉴 |
|---|---|---|
| 写入侧 | **全量落库**（无批判路由） | ✅ RPE 写入门控（最大差距） |
| 检索侧 | sufficiency 门控（证据充分跳过扩展） | 已有（检索侧 RPE 雏形） |
| 遗忘 | τ 衰减（时间驱动） | RPE 补充（价值驱动遗忘） |
| 整合 | 梦境（社区+摘要+Schema 进化） | 对应「深度记忆演化」（已有） |
| 效用评估 | confidence（抽取置信度） | 惊奇度 + 长期效用双信号 |

## 三、SHM RPE 写入门控设计（最小实现）

### 写入管线（api/routes/write.py 新增 RPE 批判）
```
新 episode 写入
  → ① 语义惊奇度（surprise）：embed 查询库中 top-1 相似记忆的相似度
       surprise = 1 - max_sim（高相似 = 低惊奇 = 重复信息）
  → ② 长期效用（utility）：实体丰富度（命中 EntityNode 数）× 内容长度
       utility = 0.5×实体数 + 0.3×min(长度/200,1) + 0.2×时间锚有无
  → ③ 批判路由：
       surprise > 0.45 且 utility > 0.5  → 深度写入（EpisodeNode + 梦境候选）
       surprise > 0.25                    → 快速缓存（轻量节点，τ 加速衰减）
       否则                                → 忽略（仅强化已有记忆 τ/置信度）
  → ④ RPE 更新：写入后 surprise 回落（已记忆），下次同类输入忽略
```

### 配置（WriteGateConfig）
```
write_gate:
  enabled: false          # 默认关零回归（产品端实验开关）
  surprise_deep: 0.45     # 深度写入阈值
  surprise_cache: 0.25    # 快速缓存阈值
  utility_min: 0.5        # 效用下限
  rpe_decay: 0.9          # RPE 更新衰减
```

### 评测验证设计
- LoCoMo 灌库**不经过** RPE 路由（评测全量灌库语义）——RPE 是产品端效率改进
- 验证指标：真实对话写入量减少 %（忽略/缓存比例）+ 检索准确率保持（生产 A/B）
- 评测侧代理：Token 成本对比（检索上下文长度 × 过滤率）

## 四、与 AtomicFact A2 的关系
- **AtomicFact A2** = 检索侧注入纪律（相关性 + 时间仲裁）——正在全量验证
- **D-MEM RPE** = 写入侧批判路由（源头过滤）——互补：写入少噪 → 检索少干扰
- 共同哲学：**批判性路由（先判断价值再行动）**——对齐 CC「先把信号测干净」

## 五、实施步骤（待确认）
1. Step 1：WriteGateConfig + 写入管线 RPE 批判（api/routes/write.py）
2. Step 2：路由决策（深度/缓存/忽略）+ RPE 状态更新
3. Step 3：测试（单元 + 生产 A/B 冒烟）
4. Step 4：产品端验证（写入量减少 + 检索保持）
5. 与 v6.4.0 主线（AtomicFact A2 全量 → 评测校准 strict×3）并行

## 六、风险与边界
| 风险 | 缓解 |
|---|---|
| 误判关键信息（惊奇度误杀） | 默认关 + 阈值保守 + 快速缓存兜底（不直接忽略重要实体） |
| 评测不受影响（LoCoMo 灌库绕开） | 产品端独立验证（A/B） |
| 计算开销（每写入一次相似度查询） | 复用已有 embed + HNSW 检索（~ms 级） |
| RPE 状态维护 | 用已有置信度/τ 字段（不新增存储） |
