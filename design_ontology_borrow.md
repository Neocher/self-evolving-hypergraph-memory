# 本体论增强智能问数——借鉴手册（2026-08-23 用户提供）

> 来源：用户消息「同步借鉴如下内容」。目标：把 Text-to-SQL 本体论增强方法论吸收进 SHM 本体/记忆体系。

## 一、核心思想
把问数从「一次翻译」重构为「两次翻译」：
- **本体**承担语义解析（消歧、映射、口径补全）
- **LLM** 承担 SQL 生成
- **确定性归本体，模糊性归 LLM**

## 二、离线基建：本体构建（可执行流程）
1. **输入物料**：schema + 字段注释、历史 SQL 仓库、指标定义文档、数仓建模文档
2. **五步半自动流水线**（LLM 抽取 + 人工校验）：
   | 步骤 | 操作 |
   |---|---|
   | ① 候选指标抽取 | 历史 SQL 喂 LLM，抽取 SELECT 聚合表达式聚类（300 SQL → 47 条 SUM 聚成"客单价"） |
   | ② 词汇归并 | 同义词聚类（客单价/ATV/人均消费），嵌入向量 + LLM 双重判别，人工审核边界 |
   | ③ 维度层级构建 | 维度表数据值自动归纳层级（GROUP BY 大区/省/市，检查严格树，异常转人工） |
   | ④ 口径审定 | LLM 生成口径草案（名称/公式/过滤/适用维度），业务评审签名，owner 必填 |
   | ⑤ 持续运营 | 每周漂移检测，新 SQL 模式无法覆盖告警 → 触发增补 |
   - 工作量：60% 对齐裁决 / 30% 运营 / 10% 画本体
3. **七大构件**（YAML 落地）：
   - meta：本体定义、版本
   - 实体映射：物理表 + **grain 粒度**（订单表"一行一笔订单"——粒度错则聚合翻倍）
   - 关系：写死 join 路径 + type 基数（1:N/N:1 决定去重策略）
   - 指标：公式（"已支付订单 pay_amount 求和"非"销售额"）+ synonyms + filter（退款不算）+ owner
   - 维度：层级展开（华南=广东+广西+海南）+ 概念值映射（"钻石客户"→'DIAMOND'）+ values 枚举
   - 规则：可执行断言，校验层拦截错误 SQL
   - 派生指标：复杂查询表达为已有指标组合
4. **存储选择**：大多数 YAML/JSON 够用；OWL 过度上架是技术自嗨；图遍历需求才属性图
5. **治理**：语义化版本（破坏性升 minor/新增 patch）+ PR 评审 + 回归测试；MCP 暴露工具（list_metrics/get_metric_definition/expand_dimension/validate_sql）而非整份本体进 prompt；最小可用本体（高频+可形式化+有 owner，长尾 LLM 兜底）；漂移检测 + 季度年审

## 三、在线执行：四步链路
1. **语义解析（Schema Linking）**：NER + embedding 双向召回（宁过度）→ 约束消歧（本体相关性/排他 + 上下文重排）→ 确定性 mapping 落表/列/join（可审计）
2. **上下文组装**：Schema 裁剪（裁剪即消歧）+ Join 路径注入 + 口径注入（公式/过滤/适用维度）+ 维度值展开 + few-shot 检索（2~3 对）
3. **SQL 生成**：高频（top 指标×top 维度）→ IR 编译保确定性；长尾 → LLM
4. **三级验证**（便宜→贵）：编译期（口径公式模式匹配，不符打回附正确公式）→ 运行期（语法/EXPLAIN/空结果）→ 语义期（量级常识/历史区间偏差）；最小干预（确定性补丁 → 回生成层 → 降级澄清）

## 四、避坑清单
- 勿过度建模（本体比代码复杂没人维护）
- 勿当全自动（口径谁说了算 = 业务 owner）
- 勿与 schema 脱节（新表不更新 = 系统性静默错误）
- 分工铁律：LLM 理解语言，本体锚定语义；价值靠「检索（上行）+ 校验（下行）」两头夹
- 成本：40% 构建 / 60% 运营
- 成功标准：口径吵架少了多少、报表打架少了多少、错误是否可拦截（非本体多完整）

## 五、终局判断
- 范式迁移：本体从 prompt 组成部分 → **工具接口本身**（MCP 暴露语义层工具，Agent 在接口上规划）
- 四个叙事泡沫：①本体推理能力跃迁（主链路不跑 OWL）②一次构建长期受益（腐烂成本）③本体让系统理解业务（只是查询结构化定义）④全自动本体构建（口径审定天然是人）
- **本体瘦身**：词汇映射被 LLM 语义吸收，不可替代的只剩**指标口径（治理资产）+ 维度层级（计算依赖）**——根本是数据治理

## 六、SHM 借鉴映射（初步）
| 手册构件 | SHM 现有 | 差距 |
|---|---|---|
| 候选指标抽取 | ontology_evolution（梦境聚合 topics → 新类型） | 无 SQL 仓库场景；可借鉴"从历史查询聚类" |
| 词汇归并/同义词 | attr_aliases（v5.50 别名合并） | 缺嵌入+LLM 双重判别 |
| 维度层级 | ontology 类型 + extended_types | 无层级树（华南=广东+广西） |
| 指标（公式/filter/owner） | PropertyVerNode（属性值版本） | 缺公式/过滤/owner 治理字段 |
| 关系（join 路径+基数） | REL 谓词边（PARTNER_WITH 等） | 无基数标注 |
| 规则/校验 | write_validate + ConflictNode | 无 SQL 校验场景 |
| 派生指标 | 无 | 缺"已有概念组合"表达 |
| MCP 工具暴露 | shm-tools（read_file 等） | 缺 list_metrics/get_metric_definition 类语义工具 |
| 三级验证 | 检索 degraded/熔断 | 无生成后验证链 |

**最相关借鉴点**（SHM 本体/记忆语境）：
1. **两次翻译**：本体锚定语义（消歧/映射），LLM 补模糊——与 SHM v72 本体论组织同思想
2. **口径公式注入**：PropertyVerNode 补公式/过滤/owner → 治理资产
3. **MCP 工具接口**（终局方向）：本体从 prompt 组成 → 工具接口——SHM 可暴露记忆本体工具
4. **避坑**：勿过度建模（SHM v71/v72 塞池竞争教训一致）、勿全自动（口径审定要人）、勿与 schema 脱节（漂移检测）
