## 设计两个剩余 Tier 2 组件

### A. Dashboard (Web UI)
单页面仪表盘，FastAPI + Jinja2 + Chart.js：
- /dashboard → 概览页（记忆总数/FAISS大小/梦境状态/延迟）
- Chart.js CDN 加载（不捆绑）
- 顶部Tab: 概览 | 记忆 | 梦境 | 日志

### B. VectorDB 可插拔抽象
当前硬编码 FAISS。抽象 VectorDB 接口：
- BaseVectorStore ABC: add/remove/search/dimension
- FaissStore 实现（当前）
- 配置: SHM_VECTOR_STORE=faiss|qdrant|milvus

### 输出
每个组件的文件清单、代码量、实施路径。
