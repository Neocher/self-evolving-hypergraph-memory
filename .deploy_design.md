## 设计 SHM 一键部署方案

### 目标
让任何人在一台 Linux 服务器上通过一行命令部署完整的 SHM 系统。

### 当前状态
- 项目在 /home/admin/shm/
- FastAPI 服务 :8000
- 依赖 RyuGraph (内部), FAISS, 各种 Python 包
- 已有 requirements.txt
- 已有 Dockerfile (hermes-df/Dockerfile 但那是研究管道)

### 设计要求
1. 一行命令部署
2. 最小依赖
3. 零配置可启动
4. 不修改现有核心代码

### 需要读取的文件
1. /home/admin/shm/requirements.txt
2. /home/admin/shm/shm/_version.py
3. /home/admin/shm/api/app.py (前50行看导入)
4. /home/admin/shm/hermes-df/Dockerfile

### 输出要求
1. 新增什么文件（每个文件的作用）
2. 各方案的实现路径
3. 推荐最短路实施方案
4. 代码量估算
