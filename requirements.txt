# SHM 运行依赖
#
# ⚠️ 存储引擎 GraphLite SDK (graphlite_sdk, Apache-2.0) — 【仅迁移需要】：
#   v6.0.0 起默认后端为 OverGraph（graphlite_sdk 不在 PyPI，需源码构建）。
#   仅 data/migrate_graphlite_to_overgraph.py 读取旧 GraphLite 库时需要；
#   迁移完成后可整体移除。构建方式（如需）：
#     git clone https://github.com/Neocher/GraphLite ~/GraphLite   # 修复版 fork（UTF-8 lexer 4452a96+）
#     cd ~/GraphLite && source ~/.cargo/env && cargo build --release -p graphlite-ffi
#     export GRAPHLITE_BINDINGS=~/GraphLite/bindings/python
#     export GRAPHLITE_SDK=~/GraphLite/sdk-python/src
#
# 以下均为 PyPI 依赖：
numpy>=1.24.0
sentence-transformers>=2.2.0
structlog>=23.0.0
prometheus-client>=0.17.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
scikit-learn>=1.3.0
cdlib>=0.4.1
leidenalg>=0.10.0
blake3>=1.0.0
pyyaml>=6.0
httpx>=0.27.0
mcp>=2.0.0
overgraph>=0.17.0  # 存储引擎（纯 Rust 嵌入式图数据库，overgraph.io；v6.0.0 起唯一后端）
