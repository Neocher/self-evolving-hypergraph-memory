# SHM 运行依赖
#
# ⚠️ 存储引擎 GraphLite SDK (graphlite_sdk, Apache-2.0) 不在 PyPI：
#   需要源码构建（Rust）后通过环境变量注入 sys.path：
#     git clone https://github.com/GraphLite-AI/GraphLite ~/GraphLite
#     cd ~/GraphLite && source ~/.cargo/env && cargo build --release -p graphlite-ffi
#     export GRAPHLITE_BINDINGS=~/GraphLite/bindings/python
#     export GRAPHLITE_SDK=~/GraphLite/sdk-python/src
#   详见 README 「Installation」章节。
#
# 以下均为 PyPI 依赖：
faiss-cpu>=1.7.4
numpy>=1.24.0
sentence-transformers>=2.2.0
structlog>=23.0.0
prometheus-client>=0.17.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
scikit-learn>=1.3.0
cdlib>=0.4.1
leidenalg>=0.10.0
blake3>=1.0.0
pyyaml>=6.0
httpx>=0.27.0
mcp>=2.0.0
