# SHM 写串行化队列 — 设计任务 (v5.23)

## 背景 (实测确认, 2026-08-11)

- 单写 237ms 正常; 8 并发写 3.2s/条 (0.3 req/s) — 全部串行排队
- 根因: 单事件循环 + 写路径 GraphLite 同步直调 → 每个写请求阻塞整个 loop
- 检索路径已用 asyncio.to_thread (26ms), 但写路径 to_thread 有风险:
  GraphLite session 是 thread_local tokio runtime, 写操作(INSERT/事务)
  跨线程实测挂起 (读操作跨线程 OK, 已由检索验证)
- GraphLite 存储: 16 分区 RwLock (读读并行/读写分区), 单写者上限 ~5 req/s

## 目标

写串行化队列: 写请求入队, 专用写线程串行消费, 事件循环不再阻塞。
预期:
- 8 并发写: 3.2s/条 → ~200-400ms/条 (排队 + 写, 不阻塞 loop)
- 吞吐: 0.3 → ~3-5 req/s (GraphLite 单写者物理上限)
- 读请求完全不受写阻塞影响

## 设计

### 核心: 专用写线程 + asyncio.Queue

```
写请求 (async handler)
  → await write_queue.submit(fn)   # 入队 + 等 future
      ↓
asyncio.Queue[WriteTask]
      ↓ (专用写线程消费, 串行)
  GraphLite 写操作 (同步, 单线程, 天然安全)
      ↓
  future.set_result() → handler 恢复
```

### 组件

1. **WriteQueue (新文件 core/write_queue.py)**:
   - `WriteTask`: dataclass(fn, args, kwargs, future: concurrent.futures.Future)
   - 后台线程: threading.Thread(daemon=True), 循环 queue.get() → fn(*args, **kwargs)
     → future.set_result / set_exception
   - `async def submit(fn, *args, **kwargs)`: 创建 Future, 入队, await
     (用 asyncio.wrap_future 或 loop.run_in_executor 桥接)
   - 优雅关闭: sentinel 退出
   - 注意: 写线程内所有 GraphLite 调用都在同一线程 → 无 thread_local 冲突

2. **集成点 (api/routes/write.py)**:
   - create_episode / write_sensory / write_multimodal / create_episodes_batch 的
     GraphLite 写调用 (create_episode / ensure_session / link_to_session /
     create_visual_node / execute_cypher INSERT) 改为:
     `await deps.write_queue.submit(deps.graphlite_store.create_episode, data)`
   - **顺序依赖**: create_episode → ensure_session → link_to_session 是同一
     task 内三个 submit, 队列保证 FIFO 顺序 → 天然有序 ✓
   - 超边创建 (query_cypher 读) 可留事件循环 (读跨线程安全) 或也入队

3. **Services (api/deps.py 或 app.py)**:
   - 注入 write_queue 单例, 生命周期跟随 app (startup 创建, shutdown 停止)

4. **超时/熔断**: 写队列堆积时 (如 >100 任务) 拒绝新写 (503 或 QUARANTINE),
   防背压; future 等待带超时 (如 30s)

## 约束

- 版本 bump: 5.22.0 → 5.23.0 (四处同步: shm/_version.py + pyproject.toml +
  VERSION + README.md)
- 不动: P0-1 R2 缓存 / P0-2 defense 锁 / P1-1 批量合并 / P1-2 索引 / P2-1 梦境错峰
- 测试禁删真实记忆 (仅 bench 测试 ns)
- 新增测试: 并发写队列 FIFO 顺序、异常传播、超时拒绝
- 验收: 8 线程并发写 80 条, avg < 500ms/条, 读请求不受影响
  → 【Codex F3 重定义 2026-08-11】绝对 avg<500ms 与物理矛盾（单写 237ms ×
    80 条串行 → 平均等待 ≈8s）。验收口径改为两项：
    ① 读不受写阻塞（事件循环心跳 gap < 0.12s，队列提交期间）；
    ② 队列零额外开销：8 并发 × 80 条真实写总耗时 ≈ N × 单写耗时（串行
    基线，±50% 容差）——端到端延迟由 GraphLite 单写者物理上限约束，
    本机实测 0.3ms/写 → 80 条排队写 151ms vs 串行基线 281ms（无放大）。
- commit 三段式, 不 push

## 输出要求

- 改了哪些文件、关键 diff 摘要
- 测试结果 + 并发基准前后对比
- 服务重启由 Hermes 统一管理, 禁止自启服务进程
