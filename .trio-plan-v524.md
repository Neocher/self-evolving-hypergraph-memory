I have complete ground truth across all write paths. Here is the full architecture plan.

---

# 三体协奏 · 阶段 1 — 架构思考者输出：v5.24 写路径队列扩展

**范围**：`.trio-task-v524.md` + 全仓写调用清单核验（已逐一 Read 目标文件，未委派）

---

## ⚠️ 0. 先说的两个前置判定（决定方案走向）

### 判定 A：`qsubmit` 是唯一的入队入口，但 GatewayAPI 与路由不在同一模块

`qsubmit` 定义在 `api/routes/_deps.py:161`，接收 `deps: Services` 作为第一参数。**所有调用方共享同一个 `Services` 实例**（`app.py` `_init_services()` 构造 → `init_services(svc)` 注入全局），因此：
- `gateway_api.py`、`visual.py`、`communities.py`、`ontology.py`、`hyperedges.py`、`app.py`、`hebbian.py` **都能直接复用 `qsubmit`**——只需从 `api.routes._deps` 导入，不需要任何新基建。
- `qsubmit` 已封装 503 语义（队列满/关闭/超时 → `HTTPException 503`），且**队列不存在时回退同步直调**（`_deps.py:169-171`），测试环境 `Services()` 无 `write_queue` 时行为不变。

### 判定 B：核心风险不是"写路径没入队"，而是**写-写跨线程并发**

v5.23 的 F1 冒烟只验证了"loop 读 + 写线程写"并发（`test_write_queue_real_graphlite.py`）。v5.24 把 hebbian + gateway + 4 个路由全入队后，**所有写都收敛到写线程**，loop 线程不再有写——这正是消除写-写并发的正确方向。但有两处**残余 loop 线程直写**必须处理，否则写-写并发仍然存在（§3.4 详述）：
1. **`_process_embed_queue` 里的 hebbian `update()`** — 仍在 loop 线程同步调 `execute_cypher`
2. **`_persist_dream_state`（app.py 闭包）** — 梦境调度器在 loop 线程回调，同步调 `execute_cypher`

---

## 1. 各写路径接入队列的具体方案

### 1.1 gateway_api（async 方法内）→ `await qsubmit(...)`

`gateway_api.py` 有 **3 处写**，都在 async 方法内：

| 位置 | 现实现 | 改造 |
|---|---|---|
| `write_sensory` L114-124 | `create_episode` / `ensure_session` / `link_to_session` 同步直调 | 改 `await qsubmit(self._svc, ...)`，镜像 `write.py:117-128` |
| `store_episode` L197-216 | `create_episode` / `ensure_session` / `link_to_session` | 改 `await qsubmit(self._svc, ...)`，镜像 `write.py:485/505-506` |
| `store_multimodal` L352-383 | `create_visual_node` / `create_episode` / `ensure_session` / `link_to_session` | 改 `await qsubmit(self._svc, ...)`，镜像 `write.py:312/325/334-335` |

**关键点**：
- `GatewayAPI.__init__` 持 `self._svc` = 同一个 `Services`，`qsubmit(self._svc, ...)` 直接可用。
- **顺序依赖**：`create_episode` → `ensure_session` → `link_to_session` 在同一个 async 方法内 **await 顺序调用**，FIFO 保证顺序（与 v5.23 write.py 相同模式）。
- **读取留 loop**：`store_episode` 里的 `get_episode` 不存在；`write_sensory` 的 `_sensory_buffer` 是内存环形缓冲，不碰 GraphLite。无读操作需要移动。
- **异常处理**：`store_multimodal` 里 `create_visual_node` 有 try/except 包裹（非致命），`qsubmit` 抛 503 会被现有 except 捕获，行为不变。`store_episode` 的 `create_episode` 无 try/except——`qsubmit` 503 会传播到 a2a_server 路由（`gateway/a2a_server.py:180-196` 无 catch），HTTP 层返回 500。**需在 a2a_server 或 GatewayAPI 层 catch 503 → 返回 503**（§5 风险 R3）。

### 1.2 visual.py → `await qsubmit(...)`

`visual.py:59` `create_visual_node` 是唯一写调用，在 async 路由 `create_visual_memory` 内。改 `await qsubmit(deps, deps.graphlite_store.create_visual_node, {...})`，并导入 `qsubmit`。

- 读取（`get_visual_node` / `get_visual_nodes` / heatmap）留 loop——v5.23 已验证读跨线程安全。
- 顺序：单次写，无顺序要求。

### 1.3 communities.py → `await qsubmit(...)`（3 处写）

| 位置 | 现实现 | 改造 |
|---|---|---|
| `resolve_conflict` L176-180 | `execute_cypher("SET c.resolved = true")` | `await qsubmit(deps, deps.graphlite_store.execute_cypher, cypher, params)` |
| `resolve_all_conflicts` L196-200 | `execute_cypher("SET c.resolved = true" 全量)` | `await qsubmit(deps, ...)` |
| `reconcile_conflict` L289-293 | `update_with_version(...)` | `await qsubmit(deps, deps.graphlite_store.update_with_version, node_id, write_data, expected_version)` |

**关键点**：
- `reconcile_conflict` 的 `reconciler.resolve()` 是**纯内存** OCC 检测（读节点版本走 `ConflictDetector`，用 `graphlite_store.get_episode` 读——留 loop），只有最后 `update_with_version` 是写。**必须把 `reconciler.resolve()` 留在 loop，只把 `update_with_version` 入队**（§3.2 详述）。
- `update_with_version` 内部做"读 version → SET"，是**复合闭包**（`graphlite_store.py:410-449` 两段式）。**整个方法作为一个 submit 入队**，读写在写线程内原子，版本检查不会被并发写撕裂（这正是 v5.23 组合闭包模式的复用）。**禁止**拆成两个 submit。
- 顺序：单次写，无顺序要求。

### 1.4 ontology.py → `await qsubmit(...)`（6 处写，实为 6 个闭包）

| 位置 | 现实现 | 改造 |
|---|---|---|
| `batch_relations` L313-343 | 每三元组 6 次 `execute_cypher`（subj MATCH+INSERT / obj MATCH+INSERT / 边 MATCH+INSERT） | **包成单个闭包** `_upsert_batch_relation(store, subj, obj, rel_type, rel)` 整体入队 |
| `ontology_discover_apply` L278 | `engine.apply_to_ontology(result, auto_register=True)` | 见 §3.5——**实为内存注册，不入队** |

**关键点**：
- `batch_relations` 是**每个三元组** `subj MATCH+INSERT` + `obj MATCH+INSERT` + `边 MATCH+INSERT` 共 **6 次 execute_cypher**。若拆成 6 个 submit，每个三元组占 6 个队列槽位 + 6 次写线程调度；且 subj/obj/边的存在性检查若拆开，会读到中间态。**推荐包成单个闭包** `_upsert_batch_relation(store, subj, obj, rel_type)`，内部同步完成 6 次 execute_cypher（写线程内原子，与 write.py 的 `_upsert_ontology_entity` / `_upsert_ontology_relation` 同风格），整体入队。
- `ontology_discover`（L205）只读扫描，`ontology_discover_apply` 的 `apply_to_ontology` **不写 GraphLite**（`core/entity_discovery.py:639` 是内存注册到 `OntologyService`，`ontology_v2.py` 无 GraphLite 写）。**确认不入队**，但需在实现时 grep 验证 `apply_to_ontology` 无 `execute_cypher` 调用（§3.5 详述）。
- 顺序：`batch_relations` 闭包内 6 次写有严格顺序（先建 subj/obj 节点再建边），闭包整体入队天然保证。

### 1.5 hyperedges.py → `await qsubmit(...)`（1 处写）

`hyperedges.py:41-53` `create_hyperedge` 三个分支（EPISODE / SEMANTIC / TEMPORAL）都调 `deps.hyperedge_manager.create_*_hyperedge`。改：
```python
edge = await qsubmit(deps, deps.hyperedge_manager.create_episode_hyperedge,
                     member_ids=req.member_ids, topic=req.topic)
# ...其余两个分支同理
```
`create_*_hyperedge` 内部 `_persist_hyperedge` 已经 v5.23 改走 `execute_cypher`（fail-loud），作为闭包整体入队，写线程内完成 HyperedgeNode INSERT + 批量边 INSERT。`ValueError`（成员 < 2）由 submit 传播 → 现有 `except ValueError → 400` 处理不变。

- 读取（`get_hyperedge` / `get_hyperedges_by_node` / `list_hyperedges`）留 loop。
- 顺序：单次写。

### 1.6 hebbian `_persist_batch` → 入队（v5.24 最高优先级）

**调用链**（任务书已确认）：
```
api/app.py:590 _embed_poll_loop (loop 线程, 每5s)
  → api/routes/write.py:596 _process_embed_queue(svc)  ← 同步调, loop 线程
    → write.py:786 deps.hebbian_updater.update({episode_id:1.0}, get_all_connections())
      → core/hebbian.py:129 _persist_batch(updates)
        → hebbian.py:143-157 execute_cypher(SET/INSERT HEBBIAN_CONNECTION)
```
**方案（二选一，推荐 A）**：

**方案 A（推荐）— `_process_embed_queue` 内 hebbian 整体入队（最小侵入）**：
```python
# write.py _process_embed_queue 内, 替代 L785-790:
if deps.hebbian_updater and deps.graphlite_store and deps.write_queue is not None:
    conns = deps.graphlite_store.get_all_connections()   # 读, 留 loop
    await qsubmit(deps, _run_hebbian_update, deps, {episode_id: 1.0}, conns)

def _run_hebbian_update(deps, active, conns):
    deps.hebbian_updater.update(active, conns)  # 在写线程内调 _persist_batch → execute_cypher
```
但 `_process_embed_queue` 是**同步函数**（非 async），不能 `await`。**必须改 async** 或改用 `asyncio.get_event_loop().create_task`。看调用方：`app.py:596` `_process_embed_queue(svc)` 在 async loop 内同步调用，改 async 后 `await _process_embed_queue(svc)` 即可（`_embed_poll_loop` 是 async 函数）。

**方案 B — hebbian 内部注入队列**：给 `SparseHebbianUpdater` 加 `write_queue` 引用，`_persist_batch` 内 `self._write_queue.submit(...)`。侵入 core 层，且 `_persist_batch` 是同步方法无法 await——需要回调/事件循环桥接，复杂度高。

**推荐 A**：改动集中在 `write.py` + `app.py` 两个文件，不碰 core/hebbian.py 的纯逻辑。**但注意**：`_process_embed_queue` 变成 async 后，`api/routes/__init__.py:24` 和 `api/_routes.py:13` 的导入不变（函数名不变），`app.py:596` 加 `await`。

**关键设计决策**：`get_all_connections()` 是读（`graphlite_store.py:492` MATCH 全表），**留 loop**——不把读塞进写队列消耗单写者额度。`update()` 内部 `_persist_batch` 的 execute_cypher 是写，随 update 整体在写线程执行。

---

## 2. 后台路径（dream/entity_resolver/ontology_validator 内部）是否入队的判断依据

| 路径 | 调用频率 | 所在线程 | 阻塞影响 | 结论 |
|---|---|---|---|---|
| **dream `_persist_*`**（pipeline.py L1219-1389） | 低频（每 450s 梦境一次） | **已是 `asyncio.to_thread`**（`dream_pipeline.py:332-340`），**不在 loop 线程** | 无阻塞 | **不入队** |
| **dream `auto_apply_candidates`**（candidate_store L434，app.py:552 调用） | 低频（≥20 候选才触发） | `_dream_poll_loop` **loop 线程**同步调 | **阻塞**（`_persist_community_nodes` L336-419 是循环 execute_cypher） | **需决策（见下）** |
| **`_persist_dream_state`**（app.py:266-289 闭包，dream_scheduler 回调） | 低频（梦境触发/完成/强制停止） | **loop 线程** | **阻塞**（每次 ~2 次 execute_cypher） | **入队** |
| **entity_resolver.link_aliases**（entity_resolver.py:271-277） | 写入时（每次 content>80 字符） | **loop 线程**（write.py:563 同步调 `resolver.process`） | **阻塞**（每对实体 2 次 execute_cypher） | **入队（随主写入队）** |
| **ontology_validator 写路径** | | | | |
| `write_validate` L1093 | 写入时 | loop 线程 | 读（MATCH 矛盾检测），不写 | 不入队（读） |
| `extract_and_relate` L750-788 | 写入时 fallback（write.py:573） | loop 线程 | **写**（每对实体 2 次 execute_cypher） | **入队（随主写入队）** |
| `sync_entity_types_to_graphlite` L612-662 | 首次 lazy | loop 线程 | **写**（循环 300+ 实体） | **入队（随主写）** |
| `_populate_relationships` L683-734 | 读时首次 lazy | loop 线程 | **写**（扫描 + RELATES_TO） | **入队（随读请求）** |
| `read_validate` L1157-1160（首次调 `sync_entity_types_to_graphlite` + `_populate_relationships`） | 每次检索首次 | loop 线程 | **写** | **入队（随检索）** |
| `_compute_topology_score` L913-947 | 检索时 | loop 线程 | 读（MATCH 路径），不写 | 不入队（读） |
| **dream_candidate_store.apply_candidate**（L197-272） | 低频（审查后 apply） | 由 API 路由/CLI 触发 | 写 | 视调用方 |
| **system.py `/index/rebuild`**（L353-396 Hebbian 重建） | 低频（显式触发） | loop 线程 | **大块写**（批量 INSERT） | **视情况（见 §2.3）** |

### 2.1 dream 主路径 — **确认不入队**（任务书已记录，我复核确认）

`dream_pipeline.py:332-340`：
```python
persist_deleted, pruned_ids = await asyncio.to_thread(self._persist_prune, graphlite_store, prune_ops)
persist_created = await asyncio.to_thread(self._persist_communities, graphlite_store, communities, dream_id)
await asyncio.to_thread(self._persist_merge, graphlite_store, merge_ops)
await asyncio.to_thread(self._persist_hyperedges, graphlite_store, communities, dream_id)
```
梦境写**已在线程池线程执行**（`asyncio.to_thread`），不阻塞事件循环。若再入队：
- 梦境写量大（几十个社区 × 每成员多语句），会**排满写队列**（max_pending=100）→ 吞掉正常请求的额度 → 503。
- 梦境超时（`wait_for 450s`）后写线程还在执行，队列积压。

**结论**：梦境主路径不入队，保持 `asyncio.to_thread`。**但这是"写线程写"与"loop 读"并发**——与 v5.23 F1 验证的组合一致（F1 是"写线程写 + loop 读"），安全。**新增风险**：梦境写线程与写队列线程**同时写** → 写-写并发。任务书说"dream 已有独立调度（P2-1 错峰）"，但 P2-1 只推迟**自动**触发，**显式触发**（`/dream/trigger`）和**手动 apply** 仍可能与写队列并发。**规避**：显式梦境触发前检查 `write_queue.pending_count()`（探测点已存在 `write_queue.py:142`），>阈值时拒绝或延迟（§5 R4）。

### 2.2 `_persist_dream_state`（app.py 闭包）— **入队**

`app.py:266-289` 闭包在 **loop 线程**被 `dream_scheduler._persist_state()` 同步调用（`dream_scheduler.py:385-391`，在 `check_and_trigger` / `trigger_explicit` / `_run_dream` finally 中调用——全部在 loop 线程）。每次 ~2 次 `execute_cypher`（MATCH + SET/INSERT）。频率低（触发/完成/force_stop），但**它在 loop 线程**，会卡 loop。**必须入队**：
```python
def _persist_dream_state(state: dict) -> None:
    if svc.graphlite_store is None:
        return
    try:
        state_json = json.dumps(state)
        # 闭包整体入队：MATCH + SET/INSERT 在写线程内原子
        if svc.write_queue is not None:
            async def _submit():
                await qsubmit(svc, _upsert_system_node, svc.graphlite_store,
                              "dream_scheduler_state", state_json)
            loop = asyncio.get_event_loop()
            if loop.is_running():
                asyncio.create_task(_submit())   # 或 loop.call_soon_threadsafe
            else:
                asyncio.run(_submit())
        else:
            # 降级：同步直调（与原实现一致）
            _upsert_system_node(svc.graphlite_store, "dream_scheduler_state", state_json)
    except Exception:
        logger.warning("Dream scheduler state persist failed (non-fatal)")
```
**注意**：`_persist_dream_state` 是**同步闭包**（调度器回调非 async），不能直接 `await qsubmit`。**必须包 async 闭包 + `asyncio.create_task`**。这是 v5.24 的一个技术难点（§4 详述 `qsubmit` 在非请求上下文的使用）。

### 2.3 system.py `/index/rebuild` — **暂不入队**（记录到任务书结论）

`rebuild_index`（system.py L353-396）显式触发时**清空全部 Hebbian 连接 + 批量重建**（几百条 INSERT）。这是**管理操作**，频率极低，且本身在 loop 线程执行（阻塞 loop 约几秒）。**暂不入队**——理由：
- 入队会占满写队列 max_pending（几百条批量 INSERT 拆开会超 100 上限，合并成一个闭包则单任务阻塞写线程几十秒，所有正常写 503）。
- 这是显式运维操作，可接受短暂阻塞。
- **记录到任务书结论**：`/index/rebuild` 保持现状，未来若需要可加"重建期间暂停写队列"。

### 2.4 entity_resolver / ontology_validator — **随主写入队（关键决策）**

任务书说"entity_resolver 若在检索路径（读）触发写 → 需入队"、"ontology_validator 的 ConflictNode 写被 write.py 调用时已入队"。

**我的判定（比任务书更细）**：

**（a）`write.py` 调用链内的 entity_resolver / ontology_validator 写** — 已在 loop 线程，**必须入队**：
- `write.py:563` `resolver.process(req.content)` → `link_aliases` 写 ALIAS_OF 边。这是**写路径**（`create_episode` 路由内）。
- `write.py:573` `deps.ontology_validator.extract_and_relate(req.content)` → 写 RELATES_TO 边。
- `write.py:448` `write_validate` → **纯读**（矛盾检测 MATCH），不入队。

但注意**调用上下文**：这两个调用发生在 `create_episode` 路由内，此时路由**已经过多个 `await qsubmit`**（create_episode 本身已入队）。`resolver.process` 和 `extract_and_relate` 内部同步调 `execute_cypher`——**这些 execute_cypher 在 loop 线程**。要入队，必须改调用方式。

**难点**：`resolver.process()` 和 `extract_and_relate()` 是**同步方法**，内部循环调 `execute_cypher`。在 async 路由里没法直接"await 入队"整个同步调用（会阻塞）。**方案**：把整个调用包成闭包入队：
```python
# write.py:563 处
if deps.graphlite_store is not None and len(req.content) > 80:
    try:
        await qsubmit(deps, _run_entity_resolver, deps, req.content)
    except Exception:
        logger.exception("Entity resolver error (non-fatal)")

def _run_entity_resolver(deps, content):
    from core.entity_resolver import EntityResolver
    resolver = EntityResolver(graphlite_store=deps.graphlite_store)
    return resolver.process(content)  # 内含 link_aliases 写, 全部在写线程
```
同理 `extract_and_relate`：
```python
# write.py:573 处
if deps.ontology_validator is not None and triples is None:
    try:
        rel_count = await qsubmit(deps, deps.ontology_validator.extract_and_relate, req.content)
        ...
```
`extract_and_relate` 内部首次调用 `sync_entity_types_to_graphlite()`（300+ 实体循环写）——整体入队后，**写线程执行 ~300 次 execute_cypher**，会阻塞写线程 ~1-2s。**可接受**（只发生一次，lazy sync），但需注意写线程阻塞期间其他写排队（`pending_count` 暴露可观测）。

**（b）`search.py` 检索路径触发 ontology_validator 写** — **需入队，但降低优先级**：
- `search.py:190` `read_validate(...)` 首次调用 `sync_entity_types_to_graphlite` + `_populate_relationships`（**写**）。
- 这是**读请求路径**（`/memories/retrieve`），首次检索时在 loop 线程同步写。**必须入队**，否则并发检索首次触发会卡 loop。
- **方案**：把 `read_validate` 整个包成闭包入队？不行——`read_validate` 内部大量**读**（矛盾检测、拓扑查询），全入队会把读塞进写队列。**更优方案**：在 `search.py` 调 `read_validate` 前，先 `await qsubmit(deps, ontology_validator.sync_entity_types_to_graphlite)` + `await qsubmit(deps, ontology_validator._populate_relationships)`（**预同步**），再在 loop 调纯读的 `read_validate`。这样写入队、读留 loop。

但 `_populate_relationships` 返回 rel_count 有用，`read_validate` 内部 `if not self._ontology_synced` 会再次调用——**需先把 `_ontology_synced` 置 True**。**推荐**：在 search.py 加 `await _ensure_ontology_synced(deps)` 闭包（入队执行 `sync_entity_types_to_graphlite` + `_populate_relationships` + 置 `_ontology_synced=True`），然后 read_validate 纯读。**但这改动 search.py 检索热路径，需谨慎**——检索是最高频路径，加两个 submit 增加延迟。**折衷**：仅当 `not ontology_validator._ontology_synced` 时做预同步（一次性）。

---

## 3. 顺序依赖分析

### 3.1 文件内多次写调用的顺序要求

| 文件 | 写调用序列 | 顺序要求 | 保证机制 |
|---|---|---|---|
| **gateway_api.write_sensory** | create_episode → ensure_session → link_to_session | **有**（link 需 session 和 episode 都存在） | 同 async 方法内 await 顺序 → 队列 FIFO |
| **gateway_api.store_episode** | create_episode → ensure_session → link_to_session | **有** | 同上 |
| **gateway_api.store_multimodal** | create_visual_node → create_episode → ensure_session → link_to_session | **有**（episode 先建，session 链接才有意义） | 同上 |
| **write.py create_episode** | create_episode → upsert_conflict → upsert_entity → upsert_relation → ensure_session → link_to_session → get_or_create_session → link_session_member | **有**（主 episode 先建，后续边/节点才能挂） | v5.23 已保证（await 顺序 + FIFO） |
| **write.py batch** | 逐条 create_episode → ensure_session → link_to_session，然后批量超边 | **有**（超边需 episode 已落库） | 同 task await 顺序 |
| **communities.reconcile** | reconciler.resolve()（内存）→ update_with_version | **有**（先检测后写） | resolve 留 loop，update_with_version 单 submit 原子 |
| **ontology.batch_relations** | 每三元组：subj MATCH+INSERT → obj MATCH+INSERT → 边 MATCH+INSERT | **有**（边依赖节点存在） | 闭包整体入队，写线程内同步 |
| **hebbian update** | 每连接：MATCH 边存在 → SET 或 INSERT | **有**（SET 需边存在） | update 整体入队，写线程内同步 |
| **app._persist_dream_state** | MATCH SystemNode → SET 或 INSERT | **有** | 闭包整体入队 |

### 3.2 关键顺序陷阱：`update_with_version` 的"读-写"复合

`graphlite_store.update_with_version`（L410-449）内部**先查 version 再 SET**。这是两段式，若拆成两个 submit：
- 第一个 submit 读 version，第二个 submit 写——中间其他写可能改了 version → 乐观锁判断基于过期快照。
**必须整个方法作为一个 submit 入队**（写线程内读-写原子）。这已经在 v5.23 的 `write_reconciler` 调用链中验证过（`write.py` 的 `reconcile_conflict` 路由）。

### 3.3 顺序陷阱：`_persist_batch` 的"MATCH 边存在 + SET/INSERT"

`hebbian._persist_batch`（L141-157）每条连接：先 MATCH 边存在性，再 SET 或 INSERT。这是**检查-写入复合**，必须整体在写线程内（入队 update 闭包天然保证），**禁止**拆成两段 submit。

### 3.4 顺序陷阱：`_persist_community_nodes` 的三阶段边操作

`dream_candidate_store._persist_community_nodes`（L303-432）是**三阶段**：清旧边 → 建新边 → 清理跨社区成员。三个阶段有严格顺序（阶段 3b 依赖阶段 3a 的 max_community_by_member 映射）。**若入队**（`auto_apply_candidates`），必须整个方法作为一个 submit。但 §2 已判定 `auto_apply_candidates` 在 poll loop 调用需决策。

**这里有个更深的问题**：`auto_apply_candidates` 在 `_dream_poll_loop`（app.py:552）同步调用，它在 loop 线程执行 `_persist_community_nodes`（几十次 execute_cypher）→ **阻塞 loop**。但 v5.23 评审遗留清单没提它，任务书也没提。**我的判定**：`auto_apply_candidates` 应**改为 `asyncio.to_thread` 或整体入队**。考虑到它可能执行几十次写且**与写队列线程并发写**（写-写风险），**推荐整体入队**：
```python
# app.py:552
applied, communities, deleted = await qsubmit(svc, svc.dream_candidate_store.auto_apply_candidates, svc.graphlite_store)
```
但 `_dream_poll_loop` 里已有多个 `await`（check_and_trigger），加 await 可行。**但注意**：`auto_apply_candidates` 内部还调 `_persist_community_nodes` 的 execute_cypher，整个入队后这些写都在写线程，**与 dream 主路径的 `asyncio.to_thread` 写线程并发**——写-写并发仍在（dream to_thread 线程 vs 写队列线程）。**这是 v5.24 无法完全消除的**（dream 主路径不走队列是设计决策）。**缓解**：dream 主路径写量大，write queue 的 `pending_count()` 探测点 + P2-1 写压力感知已存在；再补一个"auto_apply 前检查队列深度"（§5 R4）。

---

## 4. 风险：`qsubmit` 在非请求上下文（poll loop）的使用是否正确

**结论：`qsubmit` 本身设计正确，但在 poll loop / 同步回调中使用有 3 个具体风险，需要桥接层。**

### 4.1 `qsubmit` 需要 async 上下文

`qsubmit` 是 `async def`，内部 `await q.submit(...)`，而 `q.submit` 内部 `asyncio.get_running_loop()`。**只能在 async 函数中调用**。poll loop 是 async（`_dream_poll_loop` / `_embed_poll_loop` 都是 async），**可直接 await**。但：
- `_process_embed_queue` 当前是**同步函数**——改 async 即可。
- `_persist_dream_state` 是**同步闭包**（调度器回调）——不能直接 await，需要包 async 闭包 + `asyncio.create_task`。

### 4.2 风险 A：`asyncio.create_task` 从同步回调调度 — loop 归属

`_persist_dream_state` 在 loop 线程被调度器同步调用。包 `asyncio.create_task(_submit())` **必须从 loop 线程调用**（`create_task` 需要 running loop）。调度器回调确实在 loop 线程（`dream_scheduler` 的 `check_and_trigger` / `trigger_explicit` / `_run_dream` 都在 loop 线程）——**安全**。但若未来调度器改到线程池，`create_task` 会抛 `RuntimeError: no running event loop`。**防御**：`_persist_dream_state` 里判 `loop.is_running()`，非 running 时同步直调（降级）。

### 4.3 风险 B：poll loop 的 `await qsubmit` 阻塞 poll 周期

`_embed_poll_loop` 每 5s 调 `_process_embed_queue`。若 hebbian `update()` 入队后 `await qsubmit`，**poll loop 会阻塞直到写完成**（最多 wait_timeout=30s）。这**不阻塞事件循环**（await 让出 loop），但**会延迟下一次 poll**。**影响**：embedding 队列消费频率从 5s 降到"5s + 写耗时"。可接受（写耗时通常 <100ms）。**但注意**：`_process_embed_queue` 是**批量**消费（一次处理整个 `_embed_queue` 快照），如果每条的 hebbian 都 `await qsubmit`，批内串行等待写队列——**写队列也串行**，batch 的 hebbian 写会排长队。**优化**：把整个 batch 的 hebbian 更新**合并成一次 submit**（一个闭包处理 batch 内所有 episode 的 hebbian 更新），而不是每条一个 submit。

### 4.4 风险 C：poll loop 触发时队列背压 — 503 语义错位

`qsubmit` 满/超时抛 `HTTPException(503)`。在 poll loop 里 catch 到 503 意味着"写队列忙"，**不是 HTTP 请求**——503 语义无意义。**必须**在 poll loop 里 catch `HTTPException` 并记日志降级（丢弃或重试下一轮），**不能让 503 异常传播到 poll loop 外层**（会导致 `_dream_poll_loop` 的 `except Exception` 吃掉，但 `_process_embed_queue` 的 `_embed_poll_loop` 会 `except Exception: logger.exception`——**会误记 ERROR**）。**推荐**：poll loop 里用 `asyncio.wait_for(qsubmit(...), timeout=...)` + catch `HTTPException`/`TimeoutError`，记 WARNING 降级。

### 4.5 风险 D：`qsubmit` 的 `get_running_loop()` 在 gateway 独立进程

`gateway/a2a_server.py` 通过 `_build_gateway()` 调 `_init_services()` 创建独立 Services + WriteQueue，跑独立 uvicorn。A2A server 的 GatewayAPI 写调用 `await qsubmit(self._svc, ...)`——**qsubmit 拿 `self._svc.write_queue`**，这是 A2A 进程自己的 WriteQueue（独立写线程），**与主 API 进程的写队列是两个队列**。**同一 GraphLite 数据库**被两个进程的两个写线程写——**跨进程写-写并发**！这是 v5.24 的最大风险（§5 R1）。需要确认 A2A server 是否在生产中独立运行（`shm.service` 只跑主 API，A2A 是可选独立服务）。

---

## 5. 风险汇总与规避

### R1（最高）：跨进程写-写并发 — A2A 独立进程的 WriteQueue

A2A server（`gateway/a2a_server.py`）若独立运行，它有自己的 `_init_services()` → 自己的 WriteQueue + 写线程，与主 API 进程的写线程**并发写同一 GraphLite 库**。GraphLite 单写者上限（16 分区 RwLock）会退化为竞争。

**规避**：
- **方案 1（推荐）**：确认 A2A server 生产是否启用。`shm.service`（systemd）只跑主 API；A2A 若未启用，风险不存在。若启用，**A2A server 复用主 API 进程**（同一 uvicorn 挂两个 router）或**通过 HTTP 调用主 API**（`gateway/cli.py` 模式，用 `requests.post`），不建第二个 WriteQueue。
- **方案 2**：A2A 的 `_build_gateway()` 不建 WriteQueue（`svc.write_queue = None`）→ `qsubmit` 回退同步直调 → 但阻塞 A2A 自己的 loop（A2A 流量低，可接受）。
- **实现时验证**：`grep -rn "a2a_server\|8001" shm.service deploy.sh` 确认 A2A 是否随主服务启动。

### R2（高）：`_process_embed_queue` 改 async 的连锁影响

`_process_embed_queue` 是同步函数，改 async 后：
- `app.py:596` `_process_embed_queue(svc)` → `await _process_embed_queue(svc)`（`_embed_poll_loop` 是 async，OK）。
- `api/routes/__init__.py:24` / `api/_routes.py:13` 导入不变。
- **测试**：`tests/` 无直接调 `_process_embed_queue` 的测试（grep 确认）。但需检查有无 mock。

### R3（中）：GatewayAPI 写路径 503 传播到 A2A 路由 → 500

`store_episode` 的 `create_episode` 无 try/except，`qsubmit` 503 → `HTTPException` 传播到 `a2a_server.py:180-196` 的 `memory_store_episode`（无 catch）→ 返回 500。**规避**：a2a_server 的写路由加 `try/except HTTPException → 503`，或 GatewayAPI 写方法内 catch 并返回 `error` 字段（`write_sensory` 已用 `error` 字段模式）。

### R4（中）：dream 显式触发 + auto_apply 与写队列并发写

`_run_dream` 的 persist 在 `asyncio.to_thread` 线程，写队列在专用写线程，`auto_apply_candidates` 若入队则在写线程——三者可能并发写。**规避**：
- `check_and_trigger` / `trigger_explicit` 前检查 `svc.write_queue.pending_count()`，>阈值时拒绝/延迟触发（`WriteQueue.pending_count()` 探测点已存在）。
- `auto_apply_candidates` 入队后与 dream to_thread 并发——**无法完全消除**，但 auto_apply 低频（≥20 候选才触发），且 P2-1 写压力感知已推迟自动梦境。**记录到任务书结论**：接受此残余写-写并发（低频 + 写量小），后续版本若需要可给 dream persist 也走队列。

### R5（中）：`read_validate` 首次调用的写污染检索热路径

`search.py` 检索路径首次调 `read_validate` 触发 `sync_entity_types_to_graphlite` + `_populate_relationships`（写），若直接入队会加延迟。**规避**：仅在 `not ontology_validator._ontology_synced` 时做预同步（一次性），且用 `await qsubmit` 包整个预同步闭包，完成后置 `_ontology_synced=True`。之后检索纯读。

### R6（低）：hebbian `_persist_batch` 闭包内的重入

`update()` 内部 `_persist_batch` 调 `execute_cypher`——`execute_cypher` 不调其他写方法，**无重入**。安全。

### R7（低）：`_persist_dream_state` 的 `asyncio.create_task` 竞态

调度器回调期间若服务关闭（`write_queue.shutdown`），`create_task` 的 submit 会抛 `WriteQueueClosedError` → 被闭包内 `except Exception` 捕获（记 WARNING）。安全。但 `create_task` 的任务是 fire-and-forget，**可能晚于 shutdown drain**。**规避**：`_persist_dream_state` 内先查 `svc.write_queue` 的 `_closed`，关闭时降级同步直调。

### R8（低）：版本 bump 四处 + about 同步

5.23.0 → 5.24.0：`shm/_version.py` / `pyproject.toml` / `VERSION` / `README.md` + `gh repo edit --description`。CI 断言一致。

---

## 6. 实施顺序（给实现者）

按"最小侵入 → 逐步扩大 → 验证"排序：

1. **hebbian 入队（最高优先级，任务书 F6）**：改 `_process_embed_queue` 为 async，hebbian `update()` 包闭包整体入队（batch 合并为一次 submit）。`app.py:596` 加 `await`。**这是 poll loop 阻塞的根治**。
2. **app.py `_persist_dream_state` 入队**：包 async 闭包 + `create_task` + `loop.is_running()` 防御。**消除 loop 线程残余写**。
3. **gateway_api 三处写入队**：`write_sensory` / `store_episode` / `store_multimodal` 改 `await qsubmit`。a2a_server 写路由加 503 catch。
4. **visual.py**：`create_visual_node` 入队。
5. **communities.py**：`resolve_conflict` / `resolve_all_conflicts` / `reconcile_conflict` 的 `update_with_version` 入队（resolve 留 loop）。
6. **hyperedges.py**：`create_hyperedge` 三分支入队。
7. **ontology.py**：`batch_relations` 包闭包入队（6 次 execute_cypher 一个 submit）。
8. **entity_resolver / ontology_validator 随主写入队**：`write.py:563/573` 包闭包入队；`search.py` 首次检索预同步入队。
9. **dream auto_apply 评估**：`_dream_poll_loop` 的 `auto_apply_candidates` 包闭包入队（或 `asyncio.to_thread`），并加队列深度检查。
10. **版本 bump + 测试**：新增队列接入单测（gateway/visual/communities/ontology/hyperedges 各路由经队列执行）、hebbian poll 不阻塞 loop 测试、`_persist_dream_state` 入队测试。全量 `pytest tests/ -q` 绿。

---

## 7. 一句话结论

v5.23 只入了 write.py；v5.24 把 **gateway_api（3 处）/ visual / communities（3 处）/ ontology（1 个闭包）/ hyperedges / hebbian _persist_batch / app._persist_dream_state** 全部收敛到 `qsubmit`，**后台路径 dream 主路径确认不入队**（已在 `asyncio.to_thread`），**entity_resolver / ontology_validator 写路径随主写包闭包入队**，**检索路径首次 lazy 写做预同步入队**。三个技术难点需要实现者钉死：**(1) `_process_embed_queue` 改 async；**(2) `_persist_dream_state` 同步回调 → `create_task` 桥接；**(3) A2A 独立进程的跨进程写-写并发**（需先确认 A2A 是否生产启用）。