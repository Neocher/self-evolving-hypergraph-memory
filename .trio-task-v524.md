# SHM v5.24 — hebbian 入队 + 剩余写路径扩展

## 背景 (Codex 审核 v5.23 遗留, F6 + 中风险项)

v5.23 写串行化队列仅集成了 api/routes/write.py。以下写路径仍
同步直调 GraphLite (阻塞事件循环), 并发压测走这些路由会卡 loop:

1. **hebbian 持久化 (中风险, 优先)**: `_embed_poll_loop` (api/app.py:590,
   事件循环) → `_process_embed_queue` (write.py:756) → `hebbian_updater.update`
   (write.py:786) → `_persist_batch` (hebbian.py:133) → execute_cypher
   (SET/INSERT HEBBIAN_CONNECTION, 每 5s poll 循环执行)
2. **gateway/gateway_api.py**: write_sensory (L114) / store_episode (L197) /
   store_multimodal (L373) 的 create_episode 同步直调 (async 方法内)
3. **api/routes/visual.py**: create_visual_node 写路径
4. **api/routes/communities.py**: 3 处写
5. **api/routes/ontology.py**: 6 处写
6. **api/routes/hyperedges.py**: 1 处写
7. **api/app.py**: 4 处写 (启动/关闭路径, 视情况)
8. **core/ontology_validator.py** (16 处, 含 ConflictNode INSERT) /
   **core/dream_candidate_store.py** (6 处) / **core/dream_pipeline.py** (5 处) /
   **core/entity_resolver.py** (2 处) — 后台/推理路径, 评估是否入队

## 目标

所有**请求路径** (API 触发的) GraphLite 写调用收敛到 WriteQueue:
- gateway_api (async 方法内) → await qsubmit(...)
- visual / communities / ontology / hyperedges 路由 → await qsubmit(...)
- hebbian _persist_batch → 入队 (poll loop 不阻塞, 每 5s 的写不卡事件循环)

后台/推理路径 (dream/entity_resolver/ontology_validator 内部) 评估:
- dream 已有独立调度 (P2-1 错峰), 写频率低, 可暂不入队 (记录到任务书结论)
- ontology_validator 的 ConflictNode 写被 write.py 调用时已入队 (v5.23 已包
  _upsert_conflict_node), 只需确认内部没有其他直写
- entity_resolver 若在检索路径 (读) 触发写 → 需入队

## 约束

- 版本 bump 5.23.0 → 5.24.0 (四处同步 + GitHub about description 同步,
  见 AGENTS.md 版本规范第 4 条 + trio-concerto skill checklist #12)
- pytest tests/ -q 全绿
- 禁止重启服务进程 (Hermes 统一管理)
- 禁止改 v5.23 已工作的 WriteQueue 核心 (只新增调用方)
- commit 三段式, 不 push
- 测试禁删真实记忆

## 验收

1. gateway/visual/communities/ontology/hyperedges 写调用全部经 qsubmit
2. hebbian _persist_batch 经队列 (poll loop 不阻塞)
3. 并发压测走 gateway 路由 (如 write_sensory): 读不受影响
4. 全量测试绿 + 新增队列接入单测
5. 版本四处 + about 同步
