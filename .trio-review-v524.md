Now let me verify the R5 risk (read_validate re-trigger), the gateway multimodal context, and ontology `rel` variable, then run tests.
审核完成。以下为审核结论。

---

# Codex 审核 · v5.24 写路径全量收敛 WriteQueue

## 1. 各文件接入正确性 ✅

| 文件 | 接入 | 顺序依赖 | 异常处理 |
|---|---|---|---|
| gateway_api.py (3 方法) | 全部写 `await qsubmit`，kwargs 保留 | create→ensure→link 同 async 方法内 await → FIFO ✅ | store_multimodal visual 写仍在 try/except 内；store_episode 的 503 传播到 a2a_server 路由 → FastAPI 异常中间件转 503（已验证 a2a_server.py:180-182 `await api.store_episode()` 在路由内，无 catch 但转换正确）✅ |
| visual.py | create_visual_node 入队 ✅ | 单次写 | — |
| communities.py | resolve/resolve_all 的 SET + reconcile 的 update_with_version 入队；`reconciler.resolve()` 留 loop ✅ | OCC 先检测后写，update_with_version 整方法单 submit（写线程内查-写原子）✅ | — |
| ontology.py | batch_relations 6 次 execute_cypher 组闭包 `_upsert_batch_relation` 整体入队；边 INSERT 的 f-string `{{relation: $rel}}` 转义与原文一致 ✅ | 先建 subj/obj 节点再建边，闭包内同步 ✅ | 503 计入 errors（HTTP 200 + errors 计数），可接受 |
| hyperedges.py | 三分支入队，kwargs 保留；ValueError→400 传播不变 ✅ | 单次写 | — |
| write.py | entity_resolver / extract_and_relate 闭包入队；主链 v5.23 已入队 ✅ | 主 episode 写(500) → Step1(550) → Step3(577) → Phase3(591) 均 FIFO | HTTPException→WARNING 降级 ✅ |
| search.py | 首次检索 lazy 预同步 `sync_entity_types_to_graphlite` + `_populate_relationships` 入队，`_ontology_synced=True` 置位后 read_validate 纯读 ✅ | 置位防重触发（read_validate L1156 检查 flag，已验证不会二次同步）✅ | 队列忙→WARNING，验证继续 |

## 2. hebbian 入队 poll loop 上下文 ✅

- 唯一调用点 app.py:647 `await _process_embed_queue(svc)`（`_embed_poll_loop` 是 async）；`api/routes/__init__.py:24` / `api/_routes.py:13` 导入不变，无其他同步调用点（grep 全仓确认）。
- 读（`get_all_connections` MATCH）留 loop，写（`update`→`_persist_batch` SET/INSERT）在写线程，符合方案 A。
- poll loop 内 `except HTTPException` → WARNING 降级，503 语义不会泄漏到 loop（风险 C 处理正确）。
- ⚠️ 方案 B 建议"batch 合并为一次 submit"，实现为**每条 episode 一个 submit**（L814）——batch 大时 hebbian 写会串行排队。功能正确，属未采纳的优化项。

## 3. WriteQueue 核心 / 遗漏检查

- **核心未动**：`core/write_queue.py`、`api/routes/_deps.py`、`gateway/a2a_server.py` diff 为 0 ✅
- 任务书列出的写路径：gateway(3)/visual(1)/communities(3)/ontology(1 闭包)/hyperedges(1)/hebbian/app.py(_persist_dream_state + auto_apply) 全部接入 ✅
- `ontology_discover_apply`：`apply_to_ontology`→`register_entity_type` 纯内存 dict（已验证 ontology_v2.py:191）✅ 不入队正确
- `_hyperedge_sweep` 只读（query_cypher MATCH）✅
- ⚠️ **遗漏 2 处（任务归类为"评估"，未入队但未记录结论）**：
  1. `api/routes/dream.py:136` — `apply_candidate` 是 **API 请求路径写**（query_cypher DETACH DELETE / SET，loop 线程），违反任务目标"所有请求路径写收敛"，但属低频手动操作
  2. `core/dream_scheduler.py:321` — 梦境完成 auto_apply 在 loop 线程同步重写 `_persist_community_nodes`（app.py 的 poll auto_apply 已入队，此处在调度器 finally，未入队）
  - 两者符合任务书"dream 写频率低可暂不入队"的判定口径，但任务书要求"记录到任务书结论"，`.trio-task-v524.md` 无结论段，仅方案文件记录——**过程性欠账**。

## 4. 版本四处同步 + 测试质量 ✅

- 四处 5.23.0→5.24.0 一致（README/VERSION/pyproject/_version.py，__version_name__="Full-Write-Queue"）；CI ci.yml 断言 3 处一致；about 同步属发布段（约束"不 push"，未做可接受）
- 新增 21 项测试：走公共入口（TestClient 路由 + 真实 WriteQueue + GatewayAPI 公共方法），非直调内部方法；降级三态（无队列同步/队列满/队列关闭）全覆盖；并发心跳测试验证 loop 不卡；未动真实记忆（全 MagicMock）
- **本地复跑 `pytest tests/ -q` → 477 passed in 28.8s**，与 commit 声明一致 ✅

## 5. 静默失败专项 ✅

- **关键字失配陷阱**：`update_with_version(node_id=, updates=, expected_version=)` 与签名 L410 精确匹配，无 `data=` 误用；其余调用全 positional → 无被 try/except 吞掉的 TypeError
- **qsubmit 异常面**：仅抛 HTTPException（Full/Closed/TimeoutError 收敛），各新调用点均已 catch；但 `_persist_dream_state._submit` 只 catch HTTPException，`_upsert_system_node` 的 GraphLite SDK 异常（ConnectionError/QueryError）会漏入 fire-and-forget task → "Task exception was never retrieved" 日志噪音（不崩，低危）
- **降级路径**：队列 None → 同步直调语义与改造前一致（qsubmit L169-171 兜底）✅
- ⚠️ 低危：search.py 预同步在队列忙时被延迟 → `read_validate` 回退 loop 线程同步写（R5 降级路径，仅超载瞬态触发）

## 总结

方案与实现高度一致；核心要求（hebbian poll 不卡 loop、列出的请求路径全收敛、WriteQueue 核心零改动、版本四处同步、全量测试绿）全部达成。dream 相关两处写路径按"评估"口径未入队属允许范围，但未按任务书要求在任务文件记录结论。

## 通过状态: 通过

问题清单（均非阻断）:
1. [中] `dream.py:136 apply_candidate`（API 触发）与 `dream_scheduler.py:321`（梦境完成 auto_apply）loop 线程直写未入队——按任务书"dream 可暂不入队"口径可接受，但**需将结论补录到任务书**，否则后续版本丢失决策依据
2. [低] hebbian batch 未按方案合并为单次 submit，每条 episode 单独入队（大 batch 时写排队）
3. [低] `_persist_dream_state` fire-and-forget task 未兜 SDK 异常（Task exception never retrieved 噪音）
4. [低] search.py 队列忙降级时首次检索仍可能 loop 线程同步写（R5 超载瞬态）