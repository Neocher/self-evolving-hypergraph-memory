I now have complete ground truth. Let me deliver the full architecture review.

---

# 三体协奏 · 阶段 1 — 架构思考者输出：SHM 写串行化队列 (v5.23)

**范围**：审核 `.trio-task-v523.md` 设计方案，针对 5 个审查维度给出完整方案。以下为完整输出（stdout）。

---

## ⚠️ 0. 必须先说的两个前提判定（决定整个方案走向）

### 判定 A：GraphLite session 的线程模型是"共享全局"，不是 thread_local

对 `.trio-task-v523.md` 核心假设的纠正——它假设"GraphLite session 是 thread_local tokio runtime，写操作跨线程实测挂起"，并据此推出"专用写线程把调用都固定在同一线程即可安全"。但看代码事实（`graph/graphlite_store.py`）：

- **Session 是 GraphLiteStore 的实例字段**（`self._session = self._db.session("shm")`，L261），进程内全局唯一，**不是 thread_local**。
- 写路径阻塞 loop 的根因（实测 8 并发 → 3.2s/条、0.3 req/s）是 **GraphLite 存储层 16 分区 RwLock + 单写者上限**（任务书已确认），以及 **写路径根本没走 to_thread**（`api/routes/write.py` 里所有 `create_episode`/`execute_cypher` 都是**同步直调**，L65/260/273/407-416/441/490-510/571-575/609-617/692）。事件循环被同步 GraphLite 调用直接卡死。
- 读路径已用 `asyncio.to_thread`（检索 26ms）且"读跨线程 OK"——这佐证 **Session 本身跨线程可复用**（否则读也会挂）。

**推论**：如果只把写调用塞进一个专用写线程，"读仍在事件循环线程"和"写在专用线程"将**并发共享同一个 `_session`**。所以真正的设计目标不是"把写固定在一根线程上规避 thread_local"，而是 **"所有对 `_session` 的调用最终收敛到同一根线程串行执行"**。这正是设计书的核心方向，但它的**理由错了**（thread_local）——理由错了会导致集成点判定做错（见 §3）。

### 判定 B：设计书的"顺序依赖"论证不完整 — 必须查漏，否则会破坏已保证的读-写交错

`create_episode → ensure_session → link_to_session` 在一个 task 内三个 submit，FIFO 保证顺序 — **正确**。但漏掉了同一 task 内 **"写→读→写"** 的交错模式（`execute_cypher` 的 MATCH-存在性-检查 + INSERT 幂等模式），这是设计书唯一的逻辑硬伤，§3 详述。

---

## 1. 写队列设计的正确性（asyncio.Queue + 专用线程 + Future 桥接细节）

### 1.1 结构正确性 — 结论：方向对，但有两个实现级缺陷要修

设计书的结构（asyncio.Queue → 专用写线程 → Future.set_result）在**单事件循环**假设下是正确的标准 "serialized executor" 模式。FastAPI 的 uvicorn 单 worker、`api/app.py` 单 loop 都满足这个假设。

**缺陷 1：`asyncio.Queue` 非线程安全，但专用写线程要 `queue.get()` 阻塞**。`asyncio.Queue.get()` 是协程方法，不能从写线程同步调用。设计书没写清楚这一层。解决方案见 §2 桥接代码（用 `queue.Queue` 或 `loop.run_in_executor(None, ...)` + 信号量）。

**缺陷 2：Future 的 `set_result` 必须回到事件循环线程调用**。`concurrent.futures.Future.set_result` 可以在任意线程调用（它只唤醒等待者），但 **`asyncio.wrap_future` 桥接的 Future 如果在其所属 loop 之外 set_result，会抛 `RuntimeError: Future attached to a different loop`**。设计书没写这个细节。正确做法：写线程用 `loop.call_soon_threadsafe` 触发结果回调，或在 `asyncio` Future 上做线程安全桥接。

### 1.2 排队语义正确性 — 还需补三个语义

1. **重入性**：`execute_cypher` 内部**不调用其他写方法**（`get_or_create_session` 内部调 `ensure_session` 是同一队列，OK）。但 `_persist_hyperedge`（`graph/hyperedge.py` L146）**同步调 `self.store.query_cypher(...)` 来 INSERT 边** — 如果 `query_cypher` 也入队，会出现**写线程内 submit 自己 → 死锁**。必须区分"写调用入队"和"读调用留 loop"，不能一刀切。§3 详述。
2. **异常传播**：设计书说 `future.set_exception`，但没有指定**超时后异常怎么办**。补：超时等待用 `asyncio.wait_for(future, timeout)`，超时抛 `asyncio.TimeoutError`，但任务**仍然在队列/写线程里继续执行**（写线程无法取消正在执行的 GraphLite 调用）。这会导致"超时拒绝 + 实际已写入"的**语义撕裂**，必须设计为显式行为（见 §4）。
3. **容量与背压**：设计书提"堆积 >100 拒绝"，但没定义**何时检查**。补：`submit` 时检查队列深度 + 已入队未完成计数，超限抛 503；不能等消费线程发现。

### 1.3 生命周期正确性

- 写线程必须是 **daemon=True**（设计书已写）——否则 FastAPI shutdown 时非 daemon 线程阻塞进程退出。**且必须配合优雅 drain**（见 §4）。
- `sentinel` 退出正确，但要考虑**队列中残留任务**：shutdown 时写线程应 drain 完剩余任务再退出（或丢弃并 set_exception），否则在途 HTTP 请求挂死。

---

## 2. asyncio 桥接：`loop.run_in_executor` vs 纯 threading + wrap_future — 确切代码级建议

### 2.1 结论

**不推荐 `asyncio.to_thread`，也不推荐默认 `loop.run_in_executor(None, ...)` 用于长驻写任务**，理由：

- `asyncio.to_thread` 每次调用**从默认 `ThreadPoolExecutor`（max_workers ≈ min(32, cpu+4)）借线程**，无法保证"所有写操作落在同一线程"。线程池多线程并发写 GraphLite → 失去串行化语义（且 GraphLite 单写者上限本就不允许多写者）。
- 默认 `ThreadPoolExecutor` 与读路径 `to_thread`（检索）、`_MEDIA_EXECUTOR`、defense `_R2_EXECUTOR` **共享池** — 写任务卡住会饿死读路径，这正是 `write.py` L30-34 注释里已识别过的 DoS 模式。

### 2.2 推荐方案：单 worker 专用线程 + 原生 `queue.Queue` + `loop.run_in_executor` 只做一次入队桥

核心设计：**一个专用写线程，一个 `queue.Queue`（同步），事件循环侧用 `loop.run_in_executor` 把"入队+等待"整体投到一个专用 `ThreadPoolExecutor(max_workers=1)`**。这样：
- **串行化**由 `max_workers=1` 的专用 executor + 同步 queue 双重保证。
- **不借默认池**，读路径完全不受写影响。
- **不需要手写 threading + wrap_future** 的循环细节，`run_in_executor` 天然处理了 thread-safe 桥接。

```python
# core/write_queue.py
import asyncio, queue, threading, time
from concurrent.futures import Future, ThreadPoolExecutor
from dataclasses import dataclass
from typing import Any, Callable

_SENTINEL = object()

@dataclass
class _WriteTask:
    fn: Callable[..., Any]
    args: tuple
    kwargs: dict
    fut: Future          # 注意：concurrent.futures.Future

class WriteQueue:
    """串行写队列：所有 GraphLite 写调用收敛到单写线程串行执行。"""

    def __init__(self, max_pending: int = 100, wait_timeout: float = 30.0):
        self._q: queue.Queue = queue.Queue(maxsize=max_pending)
        self._executor = ThreadPoolExecutor(max_workers=1, thread_name_prefix="shm-writer")
        self._wait_timeout = wait_timeout
        self._closed = False
        self._worker = threading.Thread(target=self._run, daemon=True, name="shm-writer-worker")
        self._worker.start()

    # ── 事件循环侧：唯一入口 ─────────────────────────────
    async def submit(self, fn: Callable[..., Any], *args, **kwargs) -> Any:
        """入队并等待写线程完成，返回 fn 的结果。

        - 队列满 → 抛 WriteQueueFullError（由路由转 503）
        - 写线程执行异常 → 原样抛出
        - 超过 wait_timeout 未完成 → 抛 asyncio.TimeoutError
          （⚠️ 任务仍会在写线程继续执行，见 §4 语义）
        """
        if self._closed:
            raise WriteQueueClosedError()
        loop = asyncio.get_running_loop()
        fut: Future = Future()   # concurrent.futures.Future
        task = _WriteTask(fn, args, kwargs, fut)
        try:
            self._q.put_nowait(task)   # 满则抛 queue.Full → 路由转 503
        except queue.Full:
            raise WriteQueueFullError(f"write queue full (>{self._q.maxsize} pending)")
        # run_in_executor 保证"入队→等待"全部发生在专用单线程执行器；
        # 对 asyncio 而言是一个可 await 的 future。
        return await asyncio.wait_for(
            loop.run_in_executor(self._executor, self._await_future, fut),
            timeout=self._wait_timeout,
        )

    def _await_future(self, fut: Future) -> Any:
        """在专用执行器线程里阻塞等写线程结果——不占事件循环。"""
        return fut.result()   # 阻塞直到写线程 set_result/set_exception

    # ── 写线程侧：串行消费 ────────────────────────────────
    def _run(self) -> None:
        while True:
            item = self._q.get()
            if item is _SENTINEL:
                self._q.task_done()
                break
            task: _WriteTask = item
            try:
                result = task.fn(*task.args, **task.kwargs)
                task.fut.set_result(result)
            except BaseException as exc:      # 含 GraphLite 异常、CircuitBreakerOpen
                task.fut.set_exception(exc)
            finally:
                self._q.task_done()

    # ── 优雅关闭 ─────────────────────────────────────────
    def shutdown(self, drain: bool = True) -> None:
        if self._closed:
            return
        self._closed = True
        if drain:
            # 让写线程消费完已入队任务
            self._q.join()
        self._q.put(_SENTINEL)
        self._worker.join(timeout=5.0)
        self._executor.shutdown(wait=False)
```

**为什么 `_await_future` 用 `run_in_executor` 而不是直接手写 `loop.call_soon_threadsafe`**：`run_in_executor` 已经把"执行 fn → 返回 Future → asyncio 侧 await"整套桥接封装好了，错误处理（异常传播、loop 归属）全部正确。唯一要保证的是 **`submit` 里每次 `run_in_executor` 用的必须是 `self._executor`（max_workers=1）**，这样事件循环侧每个写请求的等待操作都排队在同一执行器上 —— 但注意：**等待操作排队在专用执行器，不代表写操作串行** —— 串行由**写线程的 `queue.get()` 循环**保证。执行器里 `_await_future` 是阻塞等结果，一个等待者拿到结果后立刻释放执行器线程，下一个 `_await_future` 才能跑 —— 但写任务本身已在写线程执行，所以**等待链自然与写线程执行链一致**。

> **风险提示（要在测试里钉死）**：`run_in_executor(self._executor, ...)` 对每个 `submit` 调用占用专用执行器线程直到写任务完成。若某次写任务长期卡死（如 GraphLite 内部死锁），`max_workers=1` 的执行器会被永久占用 → 后续所有 `submit` 在 `run_in_executor` 处排队 → **事件循环不阻塞（submit 是 await），但所有写请求全部堆积**。读路径不受影响（读不经过此执行器）。这是可接受的降级，但要作为验收项（见 §5）。

### 2.3 备选方案（不推荐）：纯 threading + `asyncio.wrap_future`

```python
loop = asyncio.get_running_loop()
fut = loop.create_future()   # asyncio.Future，线程安全桥接
task = _WriteTask(fn, args, kwargs, fut)
self._q.put_nowait(task)
# 写线程用 loop.call_soon_threadsafe(fut.set_result, result) 回调
return await asyncio.wait_for(fut, timeout=30.0)
```

**权衡**：需要手写 `call_soon_threadsafe` 回调，且 asyncio.Future 的 loop 归属问题在 uvicorn 单 loop 下没问题，但多 worker 会炸；`run_in_executor` 方案把这些细节交给标准库。选方案 A。

### 2.4 边界：写线程内的 `_await_future` 阻塞

`_await_future` 在专用执行器里 `fut.result()` 阻塞。如果写任务抛异常，`fut.result()` 抛 `concurrent.futures.CancelledError`（若调用方超时后 `wait_for` 取消外层 future）或原异常。**关键**：asyncio `wait_for` 超时**只取消外层 await，不取消 `run_in_executor` 的线程** — 线程继续 `fut.result()` 直到写线程真正完成。这正是 §4 要显式处理的"任务可能晚于超时完成"语义。

---

## 3. 集成点遗漏检查 — 这是设计书最大的缺口

### 3.1 结论：设计书说"create_episode / ensure_session / link_to_session / create_visual_node / execute_cypher INSERT 改为 submit"**过宽且过窄**

**过宽**：`execute_cypher` **不能一刀切入队**。`execute_cypher` 同时被**读和写**使用（`graphlite_store.py` L630 docstring 明确"execute_cypher 既 MATCH 也 DML"）。`communities.py:106` 是读、`communities.py:176/196` 是写、`dream_pipeline.py:1237-1270` 是写。**必须按语句语义区分**。

**过窄**：设计书只列了 `write.py` 的 4 个路由。但**写调用远不止 `api/routes/write.py`**。下面给出全仓写调用清单（已 grep 全仓，排除 tests）。

### 3.2 写调用全清单（必须入队）

| 文件 | 方法 | 是否 HTTP 写路径 | 必须入队 |
|---|---|---|---|
| `api/routes/write.py` | `create_episode`(L65,273,441,609,692) / `ensure_session`+`link_to_session`(L75-76,282-283,461-462,617-618) / `create_visual_node`(L260) / `execute_cypher` INSERT(L411-416,494-510) / `get_or_create_session`(L571) / `link_session_member`(L575) | ✅ | ✅ |
| `api/routes/visual.py:59` | `create_visual_node` | ✅ | ✅ |
| `api/routes/search.py:263` | `delete_namespace` | ✅（写删） | ✅ |
| `api/routes/communities.py:176,196,289` | `execute_cypher` DML / `update_with_version` | ✅ | ✅ |
| `api/routes/ontology.py:313-338` | `execute_cypher` INSERT | ✅ | ✅ |
| `api/routes/hyperedges.py:41-49` | `create_*_hyperedge` | ✅ | ✅ |
| `core/hebbian.py:143-153` | `execute_cypher` SET/INSERT | ⚠️ 由 embedding poll loop 调用 | **设计书漏了** |
| `core/entity_resolver.py:271-277` | `execute_cypher` INSERT | ⚠️ 在 `create_episode` 内 | ✅（随主写入队） |
| `core/ontology_validator.py:626-778,1093,1215` | `execute_cypher` INSERT | ⚠️ 在 `create_episode` 内 | ✅（随主写） |
| `core/dream_candidate_store.py:336-413` | `execute_cypher` INSERT | ❌ 后台任务 | ✅（**需决策**，见 3.4） |
| `core/dream_pipeline.py:1237-1270` | `execute_cypher` INSERT | ❌ 后台梦境 | ✅（**需决策**，见 3.4） |
| `core/write_reconciler.py:322` | `update_with_version` | ⚠️ 事务管理 | ✅ |
| `gateway/gateway_api.py:114-124,197-216,352-383` | `create_episode`/`ensure_session`/`link_to_session`/`create_visual_node` | ✅ | ✅ |
| `api/app.py:264-273` | `execute_cypher` SystemNode 状态持久化 | ⚠️ 梦境调度器回调 | ✅（**需决策**） |

**关键结论**：设计书只覆盖 `write.py` 4 个路由，遗漏了 `visual.py`、`search.py`、`communities.py`、`ontology.py`、`hyperedges.py`、`gateway/gateway_api.py` 的 HTTP 写路径，以及**后台写路径（hebbian / dream）**。v5.23 验收是"8 并发写 80 条 avg < 500ms"，但如果只改 `write.py`，并发压测走 gateway 或 visual 路由仍会阻塞 loop → **验收会失败**。

### 3.3 读调用 — 留事件循环（但要修正设计书的一个危险建议）

设计书的判断"超边创建 (query_cypher 读) 可留事件循环 (读跨线程安全)"**方向对，但要加条件**：

- **纯读** `query_cypher`（`_auto_create_hyperedges` L787-838、`_auto_create_hyperedges_batch` L864-903）→ **留事件循环** ✅。理由：读已由检索路径验证跨线程安全；超边创建是"MATCH 读 + 超边 INSERT 写"组合，**读部分留 loop，写部分（`create_*_hyperedge`）入队**。
- **但 `query_cypher` 不能全留**：`_persist_hyperedge`（`graph/hyperedge.py` L185）**用 `query_cypher` 执行 INSERT 超边边语句** — 这是写！如果 `query_cypher` 全部留 loop，这条 INSERT 就在 loop 上阻塞。**修复**：`query_cypher` 的调用不能按"方法"分类，必须按**语句内容**分类，或**给 `HyperedgeManager._persist_hyperedge` 增加入队路径**。
- **`execute_cypher` 同理**：它内部不区分读写（`graphlite_store.py` L630-649 对 DML/MATCH 一视同仁 `self._session.query()`）。所以**无法在 store 层自动判断**，必须在调用方按语义选择 `submit`（写）或直调（读）。

**设计书的最大盲点**：`query_cypher` 是"永不抛异常契约"（L680-698），它内部有 `_INFRA_EXCEPTIONS` 重试 + 熔断静默降级。**如果把写语句走 `query_cypher` 再入队，写失败的异常会被吞掉返回 `[]`**，`future.set_result([])` 后调用方误判写入成功。必须用 `execute_cypher`（不吞异常、写路径中立）做写，`query_cypher` 只做读。

### 3.4 顺序依赖的正确性 — 设计书漏了"写→读→写"交错

设计书只论证了 `create → ensure → link` 的 FIFO。但 `create_episode` 路由里有**大量"MATCH 存在性检查 + INSERT"幂等模式**（L407-416 ConflictNode、L490-510 OntologyEntity、L571-575 Session）：

```
await q.submit(execute_cypher, "MATCH (c:ConflictNode {id}) RETURN c")   # ← 这必须是读（留 loop）
await q.submit(execute_cypher, "INSERT (c:ConflictNode ...)")             # ← 写（入队）
```

**问题**：如果"MATCH 存在性检查"也入队，它读到的是**入队时的快照**；如果它留 loop，它可能读到**队列里还没执行的 INSERT** —— 两个相邻 submit 之间的**读-写顺序**需要保证"检查读"在"INSERT"之前看到一致状态。

**结论**：对"检查→写入"模式，**要么两个都入队**（MATCH 作为读入队，FIFO 保证顺序，但读入队会吃掉单写者吞吐），**要么把"检查+写入"包成一个闭包整体入队**（一次 submit 执行整个幂等操作，读写在写线程内同步完成，顺序天然正确，且 GraphLite 16 分区 RwLock 在读读并行下性能不受损）。

**推荐后者**（组合闭包入队）：`write.py` 中所有 MATCH+INSERT 幂等对，重构为 `q.submit(_upsert_conflict_node, ...)` 一个闭包，闭包内同步执行 `execute_cypher(MATCH)` + `execute_cypher(INSERT)`。这样：
- 读-写顺序在同一任务内，无交错。
- 不把读操作塞进写队列消耗单写者额度。
- 唯一注意：闭包内第二个 execute_cypher 若失败，异常经 future 抛回路由 → 路由现有 try/except 处理（`write.py` 都是非致命 catch）。

### 3.5 后台写路径决策（hebbian / dream）

- **hebbian `_persist_batch`**（`core/hebbian.py:133-159`）：由 embedding poll loop（`api/app.py` `_embed_poll_loop` L580-590，**事件循环线程内同步调用**）触发。**当前就是在事件循环上阻塞**（`_process_embed_queue` L754-759 同步调 `hebbian_updater.update`，内部 persist 同步 execute_cypher）。**必须入队**，否则写路径优化后 hebbian 仍会周期性卡死 loop。
- **dream 后台写**（`dream_pipeline.py` `_persist_communities/_persist_prune/_persist_merge/_persist_hyperedges`、`dream_candidate_store.auto_apply_candidates`）：**不能入队**。理由：梦境 `_run_dream` 里 `pipeline_fn` 在一个 `asyncio.wait_for(timeout=max_dream_duration)` 内运行，如果梦境写入也走写队列，写队列会排满（梦境写入量大），或梦境超时后写队列里积压大量任务 → 吞掉正常写请求的额度。**梦境保持现状（事件循环直调）**，但**梦境调度器加"写压力感知"**（任务书 P2-1 已规划，v5.23 不做，但队列要留出探测点：`write_queue.pending_count()` / `is_backed_up()`）。

**结论**：v5.23 集成范围 = **所有 HTTP 写路由 + hebbian 持久化** 入队；**dream 后台写** 不入队（靠 P2-1 错峰）；**`query_cypher` 留 loop 但只用于纯读**。

---

## 4. 背压 / 超时 / 优雅关闭设计

### 4.1 背压（三层）

1. **入队容量上限**（硬背压）：`WriteQueue(max_pending=100)`。`submit` 时 `put_nowait` 抛 `queue.Full` → 路由 catch → **503**（或返回 QUARANTINE 语义）。这是设计书已有。
2. **时间维度背压**：`asyncio.wait_for(future, timeout=30.0)` — 写任务在队列等待 + 执行超过 30s → `asyncio.TimeoutError`。路由应把超时写**视为失败返回 503/QUARANTINE**，同时**记录 log**（写任务仍在队列/写线程，晚些会真正落库 — 见 4.2）。
3. **语义背压（推荐新增）**：`pending_count()` 指标暴露到 Prometheus（`observability/metrics.py` 已有 `record_request`），队列深度 > 阈值时**拒绝新写**比让它们全部超时更干净。写队列深度作为**写路径健康信号**喂给 dream 调度器（P2-1 错峰的探测点）。

### 4.2 超时语义 — 必须显式设计"迟到完成"

`asyncio.wait_for` 超时**不能取消正在写线程执行的 GraphLite 调用**。所以：

- **超时 ≠ 未写入**。写任务可能晚 1-2s 完成并真实落库。调用方收到超时后**不能安全地重试**（会双重写入）。`create_episode` 用 uuid 主键天然幂等（重复 INSERT 同 id 无操作，`graph/hyperedge.py` L154-156 实证），所以**重试对 episode 安全**，但对 `link_to_session`（MATCH+INSERT 边，边级去重也幂等）安全。
- **推荐**：超时后**记录 `write_late_completion` 日志 + 指标**，不自动重试，返回 503 由客户端决定。写队列内已完成但调用方已超时的任务，其结果被丢弃（`fut.result()` 已没人等）——**没有副作用**，因为写操作本身就是提交语义（无事务回滚）。

### 4.3 优雅关闭

`api/app.py` lifespan shutdown（L593-598）当前只有 `poll_task.cancel()` + `graphlite_store.close()`。新增：

```python
# shutdown 段
poll_task.cancel()
hyperedge_task.cancel()
if getattr(svc, "write_queue", None):
    svc.write_queue.shutdown(drain=True)   # 先 drain 在途写，再关 store
if svc.graphlite_store:
    svc.graphlite_store.close()
```

顺序必须：**先 drain 写队列 → 再 close GraphLite**。反过来会导致在途写调用挂在已关闭的 session 上。`drain=True` 时 `_q.join()` 等所有已入队任务完成（上限由 `_worker.join(timeout=5.0)` 兜底防挂死）。若 >5s 未排空，sentinel 已入队、worker 退出，剩余任务 future 保持未完成 —— 在途 HTTP 请求会因超时自然失败，可接受。

---

## 5. 风险与规避

### R1（最高）：GraphLite session 跨线程并发使用导致数据损坏/挂起
- **风险**：设计书假设"写线程内所有调用同线程 → 无冲突"，但读仍在 loop 线程直调 `query_cypher` → **同一 `_session` 被读写两线程并发使用**。GraphLite 存储层 16 分区 RwLock 允许读写分区，但 **Session 对象本身（客户端侧）是否线程安全未知**——读跨线程 OK 已被检索验证，但"读+写并发"没验证过。
- **规避**：
  1. **先做冒烟测试**：写线程持续写 + loop 线程持续 `query_cypher` 读，跑 1000 次，观察挂起/异常。如果 Session 非线程安全，**读也必须入队**（读操作入队后吞吐会下降，但正确性优先）。
  2. 若读必须入队：写队列演化为"单写线程 + 读绕行"或"所有 GraphLite 访问单线程"（双队列：写队列串行 + 读队列并发，但读写都经同一线程调度）。这是方案 B。
  3. 设计书目标"读不受写影响"依赖"读写可并发"——**先验证，后承诺**。

### R2（高）：集成范围遗漏导致验收失败
- **风险**：只改 `write.py`，gateway/visual/search 仍阻塞 loop → 并发压测过不了。
- **规避**：按 §3.2 清单覆盖所有 HTTP 写路由 + hebbian。至少 gateway `create_episode`（gateway_api.py:114-124）必须覆盖，因为 MCP/A2A/CLI 全走 GatewayAPI。

### R3（高）：`query_cypher` 写语句被误留 loop 或误入队
- **风险**：`_persist_hyperedge` 用 `query_cypher` 做 INSERT（超边边）；若留 loop → 超边创建仍阻塞 loop；若入队 → 异常被吞（`query_cypher` 永不抛契约）。
- **规避**：写一律走 `execute_cypher`（或 `create_*` 方法）入队；`query_cypher` 只在**确认纯读**时留 loop。给 `HyperedgeManager._persist_hyperedge` 的边 INSERT 改走 `store.execute_cypher`（写路径）。

### R4（中）：MATCH+INSERT 幂等对的顺序撕裂
- **风险**：检查读与写入队/留 loop 混用导致读到不一致快照。
- **规避**：§3.4 组合闭包入队。所有"检查→写入"模式包成一个 submit。

### R5（中）：写线程内死锁（重入）
- **风险**：写线程内的 fn 若再调 `submit` → 队列里排队但写线程已忙 → 死锁。`get_or_create_session` 内部调 `ensure_session`（`graphlite_store.py:548`）——如果这两个都独立入队，`get_or_create_session` 的 submit 在写线程等待时又 submit `ensure_session` → 死锁。
- **规避**：**写线程内的 fn 禁止再 submit**。`get_or_create_session` 整体作为一个闭包入队（内部同步调 `ensure_session`）。在 `submit` 内检测：若当前线程是写线程 → 直接同步执行（`self._thread_ident == threading.get_ident()`），不排队。

### R6（低-中）：`run_in_executor` 专用执行器被长期写卡死
- **风险**：单个写任务卡死（GraphLite 内部）→ `max_workers=1` 执行器被占用 → 所有 `submit` 在 `run_in_executor` 排队等待，虽然不阻塞 loop 但写全挂。
- **规避**：接受降级（读不受影响）；写队列深度 + 最长写耗时暴露为指标，达到阈值告警；验收项含"写线程卡死时读路径不降级"。

### R7（低）：启动/关闭竞态
- **风险**：startup 时写队列未创建就有写请求；shutdown 时写线程未排空。
- **规避**：`WriteQueue` 在 `_init_services()` 创建并注入 `Services.write_queue`；`get_services` 已有 503 兜底（未初始化）；shutdown 顺序 drain→close（§4.3）。

### R8（低）：版本号与测试
- 版本 5.22.0 → 5.23.0 四处同步（`shm/_version.py`/`pyproject.toml`/`VERSION`/`README.md`），CI 断言一致。
- 新增测试：FIFO 顺序（多任务同 source，断言落库顺序=入队顺序）、异常传播（fn 抛错 → submit 抛同异常）、超时拒绝（mock fn 慢 → wait_for 超时 → 503）、队列满拒绝（max_pending=2 入 3 → 第 3 个抛 Full）、**写线程重入直连**（fn 内调 submit 不死锁）、**读写并发冒烟**（R1 验证）。

---

## 6. 汇总：对设计书的修正清单

| # | 设计书说法 | 修正 |
|---|---|---|
| 1 | "GraphLite session 是 thread_local tokio runtime" | ❌ Session 是 store 实例字段，进程全局。风险点是**读+写线程并发用同一 session**，需冒烟验证 |
| 2 | "asyncio.Queue + 专用线程" | ✅ 方向对，但 `asyncio.Queue` 不能从线程 `get()`。改用 `queue.Queue` + `run_in_executor(专用单worker executor)` + concurrent Future |
| 3 | "create_episode / ensure_session / ... execute_cypher INSERT 改 submit" | ❌ 过宽（execute_cypher 含读）过窄（漏 visual/search/communities/ontology/hyperedges/gateway/hebbian） |
| 4 | "超边 query_cypher 读可留事件循环" | ✅ 但 `_persist_hyperedge` 的 INSERT 走 query_cypher 是**写**，必须改 execute_cypher 入队 |
| 5 | "顺序依赖 FIFO ✓" | ⚠️ 只论证了 create→ensure→link；"MATCH 检查+INSERT"幂等对需**组合闭包入队** |
| 6 | "堆积 >100 拒绝" | ✅ 补 `put_nowait` + `queue.Full` → 503；补超时后"任务仍会落库"的迟到完成语义 |
| 7 | 未提优雅关闭 | 补 drain→close 顺序，sentinel + join(timeout) |
| 8 | 未提写线程内重入 | 补"写线程内 submit 直连同步执行"防死锁 |

---

## 7. 实施步骤建议（给实现者）

1. **先写冒烟测试**（R1）：写线程写 + loop 读，验证 Session 读-写并发安全；不安全则切方案 B（全量单线程）。
2. 新建 `core/write_queue.py`（§2.2 代码）。
3. `_init_services()` 创建 `WriteQueue` 注入 `Services.write_queue`。
4. 重构 `write.py` 所有写调用为 `q.submit(...)`；MATCH+INSERT 对包闭包。
5. 覆盖 gateway/visual/search/communities/ontology/hyperedges 写路径。
6. hebbian `_persist_batch` 入队；dream 后台写不入队，但队列暴露 `pending_count` 给 P2-1。
7. lifespan shutdown 加 drain。
8. 版本 bump 5.23.0 + 测试（§5 R8 清单）+ 并发基准（8 线程 80 条 avg<500ms，读不受影响）。

---

**一句话结论**：设计书方向正确（写串行化 + 专用线程 + Future 桥接是标准模式），但**"thread_local"前提错误、集成范围漏了半个仓、`query_cypher`/`execute_cypher` 读写未区分、MATCH+INSERT 顺序有撕裂风险**——按上述修正实施即可达标。