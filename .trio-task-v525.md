# SHM v5.25 — Codex 遗留项修复 (v5.24 非阻断问题)

## 背景 (v5.24 Codex 审核通过时记录的 4 项非阻断问题)

### 1. [中] dream apply_candidate 直写未入队
- api/routes/dream.py:136 — API 触发路径, 同步调
  store.apply_candidate(dream_id, deps.graphlite_store)
- core/dream_candidate_store.py:197 apply_candidate — 执行 PRUNE (DETACH
  DELETE) + Merge 操作 (GraphLite 每个 execute 自动提交)
- 修复: API 路由层经 qsubmit 入队 (apply_candidate 整体闭包)
- 注意: dream_scheduler.py:321 auto_apply (梦境完成后台路径) 已确认
  在 to_thread, 不入队 — 保持现状, 只在 API 触发路径入队

### 2. [低] hebbian 大 batch 逐条 submit 未合并
- v5.24 每条 episode 单独 submit (write.py:89 _run_hebbian_update),
  大 batch 时写排队
- Codex 指出: 合并会改变 active_nodes 语义 (批内 episode 互相共激),
  所以 v5.24 有意保留逐条 — **先评估**, 若语义确认不可合并则跳过
  该项并记录结论 (不强行合并)
- 【v5.25 结论】确认不可合并, 保持逐条 submit。理由: `hebbian.update()` 内
  active_ids 每条仅含 1 个 episode, 批内 episode 之间不互相共激 (两两激活度积
  只在同一批 active 内计算); 合并成单次 submit 会改变 active_nodes 语义, 产生
  batch 内连接, 非 v5.24 行为。成本: 大 batch 下 hebbian 写串行排队, 但
  _persist_batch 只在实际有更新时执行, poll loop 每 5s 消费, 常规负载下队列
  深度可接受。v5.25 无代码改动。

### 3. [低] _persist_dream_state fire-and-forget 未兜 SDK 异常
- create_task 桥接的 fire-and-forget task, 若 execute_cypher 抛
  SDK 异常 → "Task exception never retrieved" 噪音
- 修复: task 内加 try/except + logger.exception (非致命)

### 4. [低] search.py 队列忙降级时首次检索仍可能同步写
- search.py:196-200 — qsubmit 已用, 但队列忙 (WriteQueueFull) 时
  fallback 到同步直调 (ontology sync deferred)
- 修复: 确认 fallback 语义 — 队列忙时延迟/跳过 ontology sync 写
  (检索继续, 写延迟到下次) 而非同步直调阻塞 loop;
  若当前实现已经是"deferred"则确认无同步写残留
- 【v5.25 语义确认】确认现状语义: 队列忙时 `_ontology_synced` 保持 False,
  `read_validate` 首次 lazy 同步 (sync_entity_types_to_graphlite +
  _populate_relationships) 回退 loop 线程同步执行 — 这是可接受的超载瞬态
  降级: (1) 触发条件 = 队列满 (max_pending=100) 且为该进程首次检索, 两条件
  同时满足概率极低; (2) 同步写一次性执行 (之后 flag=True 永不触发); (3)
  超载瞬态下 loop 卡 ~1-2s 比"本体类型永不落库"危害小。备选方案 (read_validate
  整体 to_thread) 是检索热路径行为变更, 不建议做。v5.25 无代码改动。
- 【附加记录】A2A 独立进程跨进程写-写并发: a2a_server._build_gateway 建独立
  Services + 独立 WriteQueue, 若 A2A 独立进程运行则与主 API 进程各自有写线程写
  同一 GraphLite。非 v5.25 阻断 (A2A 生产启用与否未定); 未来启用时需改
  _build_gateway 不建 WriteQueue 或复用主进程。

## 目标

- 修复 1 (dream API 路径入队) — 必做
- 修复 3 (fire-and-forget 兜底) — 必做
- 修复 4 (search 降级无同步写) — 必做 (确认语义)
- 评估 2 (hebbian 批量合并) — 语义确认, 可合并则合并, 不可则记录结论

## 约束

- 版本 bump 5.24.0 → 5.25.0 (四处同步 + GitHub about 同步, AGENTS.md 规范)
- pytest tests/ -q 全绿
- 禁止重启服务进程 (Hermes 管理)
- 禁止改 WriteQueue 核心
- commit 三段式, 不 push
- 测试禁删真实记忆

## 验收

1. dream API apply_candidate 经队列 (PRUNE/Merge 不阻塞 loop)
2. _persist_dream_state task 异常有日志兜底
3. search 队列忙时无同步写 (检索可用, 写延迟/跳过)
4. hebbian 合并结论明确 (合并 or 记录不可行理由)
5. 版本四处 + about 同步
