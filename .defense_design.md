## 设计记忆投毒防御系统

### 参考
TMA-NM (arXiv:2606.24322) — 面向 Agent 记忆的投毒防御

### 目标
为 SHM 添加对抗性记忆写入的检测和隔离能力。

### 当前状态
- 已有 BLAKE3 审计链（仅溯源，无主动防御）
- 任何 Agent 写入的任何内容都直接进入主存储
- 无写入预检、无异常检测、无隔离区

### 设计要求 (Karpathy 风格)
1. **最小改动**: 不改核心存储，只加预检层
2. **方案 A (规则引擎)**: 5-8 条规则，纯 Python，零新依赖
   - 写入频率尖峰检测
   - 语义漂移检测（新内容与 Agent 历史内容余弦相似度 < 阈值）
   - 异常实体共现（从未同时出现的实体突然大量共现）
   - 快速重复写入（同一 Agent 在 Δt 内写入 N 次相同内容）
3. **隔离区机制**: 可疑写入进入独立子图，不参与梦境巩固，人工/自动审核后可提升
4. **默认静默模式**: 只告警不阻断，可配置为阻断模式

### 需要读取的文件
1. /home/admin/shm/api/_routes.py (写入路由)
2. /home/admin/shm/core/dream_pipeline.py (梦境巩固)
3. /home/admin/shm/gateway/gateway_api.py (统一网关)

### 输出要求
1. 新增文件/类/方法
2. 与现有写入管道的集成点
3. 代码量估算
4. 验证方法

读完文件后给出完整设计。
