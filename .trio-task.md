# 三体协奏任务: namespace 检索过滤 P0 bug 修复 + benchmark 脚本适配

## 背景

### Bug 1 (P0, 核心检索): api/routes/search.py:155 namespace 预取查询属性名错误

/memories/retrieve 端点在 142-181 行做命名空间过滤:
```python
ns_rows = await asyncio.wait_for(
    asyncio.to_thread(
        deps.graphlite_store.query_cypher,
        "MATCH (s:SessionNode {session_id: $ns})-[:SESSION_MEMBER]->(e:EpisodeNode) "
        "RETURN e.id",
        {"ns": req.namespace},
    ),
    timeout=_DEGRADE_TIMEOUT,
)
ns_set = {row[0] for row in ns_rows} if ns_rows else set()
```

**错误**: `{session_id: $ns}` 用了不存在的属性。实测 (2026-08-12 15:08) SessionNode
的属性只有 id/created_at/last_seen, 没有 session_id。该查询永远匹配 0 行 →
ns_set 恒为空 set() → 171 行 `if ns_set is not None and r.get("node_id") not in ns_set: continue`
把所有结果过滤掉 → **带 namespace 的 /memories/retrieve 永远返回 0 条**。

对照: graph/graphlite_store.py:580 get_session_memories 用 `{id: '{session_id}'}` 正确。

**修复**: `{session_id: $ns}` → `{id: $ns}`。

**影响范围**: benchmark (带 namespace 检索) 全灭; 任何按 namespace 隔离的多 agent
检索全灭; 不带 namespace 的检索不受影响 (真实记忆中文检索正常, 已实测)。

### Bug 2 (bench 脚本适配): 等待异步 embedding 队列追平

v5.24+ 写入路径异步: _embed_queue → poll loop 每 5s 消费 → 编码 → FAISS。
POST /memories/episodes 返回时向量可能未进 FAISS, 立即检索会 0 命中
(2026-08-12 实测 10 hybrid recall=0, 虽部分由 Bug 1 导致, 但时序问题独立存在)。

scripts/bench_longmemeval_shm.py 已加 wait_faiss_catchup (faiss_before + written
轮询 /health faiss_index_size), 需要审核确认。

## 需要改动的点

1. api/routes/search.py:155 — session_id → id (P0, 一行)
2. scripts/bench_longmemeval_shm.py — SHMClient.faiss_size + wait_faiss_catchup +
   run_instance 写入后调用 (已改, 审核)

## 硬性约束

1. 禁止重启/启动 SHM 服务 (由 Hermes 统一管理)
2. 只改上述 2 个文件, 禁"顺便"改其他
3. 版本 bump v5.27.3: shm/_version.py + pyproject.toml + VERSION + README.md 四处
4. 保持脚本纯 stdlib

## 验收标准

1. 写 2 条到独立 ns → /memories/retrieve (带 ns) 返回 ≥1 条 (Bug 1 修复验证)
2. 不带 ns 检索仍正常 (回归)
3. bench_longmemeval_shm.py --limit 10 --endpoint hybrid 的 recall > 0
4. 版本四处同步 v5.27.3
