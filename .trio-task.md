# 三体协奏任务: check_hermes_integration.py 兼容 shm/shm_v5 双名

## 背景

SHM 仓库的 scripts/check_hermes_integration.py 是 Hermes ↔ SHM 对接检测脚本。
v5.26.1 起官方插件标准命名为 shm_v5 (目录 ~/.hermes/plugins/shm_v5/),
但历史部署 (8月) 用旧名 shm (目录 ~/.hermes/plugins/shm/)。两者功能等价:
- 旧名: 插件目录 plugins/shm/, 类 SHMMemoryProvider, config provider=shm
- 新名: 插件目录 plugins/shm_v5/, 类 SHMv5MemoryProvider, config provider=shm_v5

当前脚本 4 处硬编码 shm_v5, 导致旧名部署下误报 FAIL (明明对接正常)。
用户要求: 检测脚本兼容双名, 任一存在且可用即 PASS, 真实反映 "SHM 已对接"。

## 需要改动的点 (scripts/check_hermes_integration.py)

1. check_plugin_dir (line ~134-162): 现在只检查 plugins/shm_v5/。
   → 兼容: shm_v5 或 shm 任一目录存在含 __init__.py 即 PASS。
   错误位置检查 (plugins/memory/shm_v5/) 保持。

2. check_plugin_loadable (line ~168-254): 现在只找 "shm_v5" in providers,
   load_memory_provider("shm_v5")。
   → 兼容: providers dict 中 shm_v5 或 shm 任一存在且 available=True 即 PASS,
      load 对应存在的那个名字 (优先 shm_v5)。

3. check_config (line ~260-351): 现在 provider == "shm_v5"。
   → 兼容: provider == "shm_v5" 或 "shm" 任一即 PASS。

4. prefetch 标题判定 (line ~408-412): 上轮已兼容双标题
   ("SHM v5 记忆检索" / "【SHM 记忆检索结果】"), 保持不动。

5. 输出文案: 动态显示实际检测到的插件名 (如 "(shm_v5)" 或 "(shm)"),
   不要写死 shm_v5。FIX 提示也相应双名化。

## 硬性约束

1. 禁止重启/启动 SHM 服务 (由 Hermes 统一管理)
2. 禁止委派给其他 agent, 直接输出结论
3. 只改 scripts/check_hermes_integration.py 一个文件, 禁"顺便"改其他
4. 版本号 bump v5.27.2: shm/_version.py + pyproject.toml + VERSION +
   README.md 四处同步 (AGENTS.md 规范: 每个实质性改动 bump)
5. 保持脚本纯 stdlib (无第三方依赖), --debug 选项行为不变
6. 测试: 脚本自身无测试框架, 至少用 python3 -c 构造双名场景验证
   (monkeypatch discover_memory_providers 返回 shm / shm_v5 各跑一次,
   以及真实环境跑一次应 6/6 PASS)

## 验收标准

- 双名场景各跑一次: 插件名 shm 和 shm_v5 都判定 PASS
- 真实环境 (本机已迁移 shm_v5): 6/6 PASS, exit=0
- 版本四处同步 v5.27.2
- 只改 1 个功能文件 + 4 个版本文件
