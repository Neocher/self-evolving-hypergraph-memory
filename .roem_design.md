## 实现时序相位旋转 (RoMem) — 替换 τ 指数衰减

### 目标
为 SHM 的 τ 指数衰减函数添加傅里叶相位嵌入支持，使记忆强度不仅能建模"越旧越弱"，还能捕获周期性模式（周/月/季）。

### 当前架构
- τ 衰减在 `core/tau_decay.py` 中实现
- FAISS 检索排序也受 τ 影响
- 梦境重放优先级也依赖 τ 值

### 设计要求
1. **方案**: 傅里叶相位嵌入 (RoMem)
   - 为每条记忆计算时间戳的相位向量 φ = [sin(ω₁t), cos(ω₁t), sin(ω₂t), ...]
   - ω 对应日/周/月/季频率
   - 记忆强度: S = α·S_τ + (1-α)·S_φ
   - S_φ = cosine_similarity(φ_now, φ_memory)
   - α 从环境变量读取（默认 0.7, 即70%旧τ + 30%相位）

2. **不改现有接口**: tau_decay.py 的 `compute_strength()` 函数签名不变，内部新增相位分量

3. **新增配置**: 
   - `SHM_PHASE_ALPHA` — 混合权重 (默认 0.7)
   - `SHM_PHASE_FREQS` — 频率列表 (默认 "1d,7d,30d,365d")

4. **零新依赖**: 纯 numpy 运算

### 需要读取的文件
1. /home/admin/shm/core/tau_decay.py — 当前 τ 衰减实现
2. /home/admin/shm/core/dream_pipeline.py — 梦境重放（τ 使用处）
3. /home/admin/shm/api/_routes.py (搜索 compute_strength 和 retrieve 路由) — 检索排序

### 输出要求
1. 修改哪些文件
2. 具体代码改动（精确到行）
3. 验证方法

读完文件后给出设计。
