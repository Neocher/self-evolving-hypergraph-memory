I have full ground truth across all five requested areas — every write path read, the v5.24 plan/review (which logged these 4 non-blocking items), the WriteQueue core, the scheduler threading, and the A2A lifecycle. Here is the complete architecture plan.

---

# 三体协奏 · 阶段 1 — 架构思考者输出：v5.25 Codex 遗留项修复

**范围**：`.trio-task-v525.md` 4 项（2 修复 + 1 语义确认 + 1 评估）+ 全仓写调用核验（已逐一 Read 目标文件，未委派）

**一句话结论先行**：v5.25 不需要"扩展队列"——需要的是 **1 处补漏入队（dream apply）+ 3 处加固（异常兜底/降级语义确认）**。v5.24 的队列收敛基本正确；剩余问题集中在 **dream 相关写路径（API apply + 调度器内 auto_apply）** 和 **异常噪音/降级语义** 上。

---

## 0. 前置判定（决定方案走向）

### 判定 A：`apply_candidate` 混合了「读 + 写」，不能整体入队

`core/dream_candidate_store.py:197` 的 `apply_candidate` 是**复合闭包**：
- **PRUNE 阶段**：`query_cypher("... DETACH DELETE")` × N — `query_cypher` 是**读 API**（`graphlite_store.py:679`，永不抛异常契约、走 `_query_retryable`），但 DETACH DELETE 是**写**。
- **MERGE 阶段**：`query_cypher("MATCH ... RETURN")` 读源 content（L241）→ `query_cypher("SET target.content...")` 写 → `query_cypher("DETACH DELETE")` 写。
- `_mark_applied` 是**纯文件 JSON 写**（不碰 GraphLite）。

**关键**：`query_cypher` 执行写语句虽然功能上能落库，但它是**读 API**（重试 + 熔断中立，走 `_query_retryable`），**不是 v5.24 语义下的"写调用收敛"**——写语句应该走 `execute_cypher`（fail-loud）或整体闭包入队。因此 `apply_candidate` 整体作为**一个闭包入队**，是唯一符合"PRUNE/Merge 在写线程"且保留原有 `query_cypher` 行为的方案。

### 判定 B：`dream_scheduler.py:321` 是 v5.25 的**第二个必改项**

`.trio-task-v525.md` 只列了 `dream.py:136`（API 路径），但 **v5.24 审核已指出同源问题**：`dream_scheduler.py:321 auto_apply_candidates` 在 `_run_dream` finally 段 **loop 线程同步执行**（`_run_dream` 是 `asyncio.create_task` 协程，`pipeline_fn` 内部 persist 走 `asyncio.to_thread`，但 `auto_apply` 是**裸同步调用**），且 `_persist_community_nodes` 是**几十次 execute_cypher 循环**（`dream_candidate_store.py:303`）。**必须入队**，否则"循环 execute_cypher 阻塞 loop"的根因没消除。

**但注意**：调度器**没有 `write_queue` 引用**（`Services.write_queue` 是 `app.py` 注入的，`DreamScheduler` 只有 `_graphlite_store` / `_state_persist_fn`）。两个方案：
- **方案 A（推荐）**：**调度器完全不做 auto_apply**——`_run_dream` finally 只 `_persist_state()`（已入队），auto_apply 交给 app.py 的 `_dream_poll_loop`（**已入队**，`app.py:598`）。这消除调度器内的写，且 poll loop 已有队列深度守卫（`pending_count() > max_pending//2` 延迟）。
- 方案 B：给调度器注入 `write_queue` + `qsubmit` 桥接（`_run_dream` 是 async，可 `await`）。侵入 scheduler，但保留"梦境完成立即 apply"的即时性。

**推荐 A**：poll loop 每 `DREAM_POLL_INTERVAL` 轮询，延迟最多一个间隔可接受；且消除"调度器线程 vs 写队列线程写-写并发"（v5.24 R4 的残余）。

### 判定 C：v5.25 不合并 hebbian batch —— 语义确认成立，跳过该项

v5.24 实现为**每条 episode 一个 submit**（`write.py:814`），每条单独 `update({episode_id:1.0}, conns)`：
- `update()`（`hebbian.py:54`）内 `active_ids` 只有 1 个 episode → **批内 episode 之间不互相共激**（两两激活度积只在同一批 active 内算）。v5.24 有意保留逐条 → 语义正确。
- **合并成单次 submit（active 含全部 batch episode）会改变语义**：batch 内 episode 两两配对更新 → 产生 batch 内连接。这不是 v5.24 的行为。
- **成本**：大 batch 下 hebbian 写串行排队（每 episode 一轮 `MATCH 边存在 + SET/INSERT`）。但 `_persist_batch` 只在实际有更新时执行，且 poll loop 每 5s 消费——**queue 深度通常低**（非满载），串行可接受。

**结论**：**记录"不可合并，保持逐条"**，不强行合并。理由：合并改变 active_nodes 语义（批内共激），非 v5.24 行为，且当前逐条在常规负载下队列深度可接受。

---

## 1. 各写路径接入队列的具体方案

### 1.1 gateway_api（async 方法内）→ 已入队 ✅，v5.25 零改动

`gateway_api.py` 3 处写（`write_sensory` L116-127 / `store_episode` L201-220 / `store_multimodal` L357-389）**v5.24 已全部 `await qsubmit`**，`self._svc` 即 Services，顺序（create→ensure→link）同 async 方法内 await → FIFO 保证。v5.25 **无改动**。

### 1.2 visual.py → 已入队 ✅，v5.25 零改动

`visual.py:61` `create_visual_node` 已 `await qsubmit`。单次写。v5.25 无改动。

### 1.3 communities.py → 已入队 ✅，v5.25 零改动

`resolve_conflict` L178 / `resolve_all_conflicts` L200 / `reconcile_conflict` L296（`update_with_version` 整体入队）均已 `await qsubmit`。`reconciler.resolve()` 留 loop 正确（纯内存 OCC 检测，`update_with_version` 写线程内查-写原子）。v5.25 无改动。

### 1.4 ontology.py → 已入队 ✅，v5.25 零改动

`batch_relations` L354 用 `_upsert_batch_relation` 闭包（6 次 execute_cypher 整体入队）；`ontology_discover_apply` 的 `apply_to_ontology` 纯内存注册不入队。v5.25 无改动。

### 1.5 hyperedges.py → 已入队 ✅，v5.25 零改动

`create_hyperedge` 三分支 L44-59 均已 `await qsubmit`，kwargs 保留，ValueError→400 传播不变。v5.25 无改动。

### 1.6 hebbian `_persist_batch` → 已入队 ✅，v5.25 记录结论

`write.py:814` `await qsubmit(deps, _run_hebbian_update, ...)`，`_run_hebbian_update` 在写线程调 `update` → `_persist_batch`。**保持逐条 submit，不合并**（判定 C）。v5.25 无改动，仅补录结论。

### 1.7 【v5.25 新增】dream.py:136 `apply_candidate` → 整体闭包入队

`dream.py:136` 当前同步直调 `store.apply_candidate(dream_id, deps.graphlite_store)`。改造：

```python
# api/routes/dream.py —— 导入 qsubmit 后
try:
    success = await qsubmit(
        deps,
        store.apply_candidate,
        dream_id, deps.graphlite_store,
    )
except HTTPException:
    logger.warning("Write queue busy, dream apply deferred (non-fatal)")
    return DreamApplyResponse(success=False, dream_id=dream_id,
                              message="Apply deferred: write queue busy, retry later")
```

**顺序依赖**：`apply_candidate` 内部 PRUNE → MERGE → `_mark_applied` 有严格顺序（先删后并，全部落库后再标记 applied）。**整体闭包入队** → 写线程内串行，顺序天然正确；`query_cypher` 读（MERGE 源 content）也在写线程，读到的是写队列串行化后的稳定状态。**禁止**拆成多次 submit（PRUNE 与 MERGE 之间会插入其他写）。

**异常语义**：`apply_candidate` 内部已 try/except 每个 op（不抛），队列满的 `HTTPException(503)` 是唯一新增异常面 → 需 catch 并返回 503 语义（deferred）。

### 1.8 【v5.25 新增】dream_scheduler.py:321 auto_apply → 移除（推荐 A）

`_run_dream` finally 段的 `candidate_store.auto_apply_candidates(self._graphlite_store)` 删除，auto_apply 完全由 app.py `_dream_poll_loop`（已入队）承担。判定 B 方案 A。

**顺序影响**：梦境完成后候选**延迟**一个 poll interval（`DREAM_POLL_INTERVAL`）才 apply。可接受——poll loop 本身每间隔轮询，且已有队列深度守卫。auto_apply 低频（≥20 候选才触发），延迟无感知。

---

## 2. 后台路径（dream/entity_resolver/ontology_validator 内部）是否入队的判断依据

| 路径 | 调用频率 | 所在线程 | 阻塞影响 | v5.24 判定 | v5.25 动作 |
|---|---|---|---|---|---|
| dream `_persist_*`（pipeline L332-340） | 低频（450s 梦境） | **`asyncio.to_thread`** | 无（不在 loop） | 不入队 ✅ | 保持 |
| **dream `auto_apply`（scheduler L321）** | 低频（≥20 候选） | **loop 线程同步**（`_run_dream` 协程内裸调用） | **阻塞**（`_persist_community_nodes` 几十次 execute_cypher 循环） | 评估口径未入队 | **【v5.25】移除 → 交 poll loop（已入队）** |
| **dream `apply_candidate`（dream.py:136）** | 低频（手动审查后 apply） | **API 路由 loop 线程** | **阻塞**（PRUNE/MERGE 循环写） | 评估口径未入队 | **【v5.25】整体闭包入队** |
| `_persist_dream_state`（app.py:55） | 低频（触发/完成/force_stop） | loop 线程（调度器同步回调） | 阻塞（~2 次 execute_cypher） | **已入队**（async 闭包 + create_task）✅ | 保持 |
| entity_resolver `link_aliases`（write.py:577） | 写入时（content>80） | loop（`_run_entity_resolver` 闭包入队） | — | **已入队** ✅ | 保持 |
| ontology_validator `extract_and_relate`（write.py:591） | 写入时 fallback | 闭包入队 | — | **已入队** ✅ | 保持 |
| ontology_validator `write_validate`（write.py:463） | 写入时 | loop 同步 | 读（MATCH 矛盾检测）+ ConflictNode 写已入队 | 读留 loop ✅ | 保持 |
| search.py `read_validate` 预同步（search.py:194-200） | 检索首次 | `qsubmit` 预同步入队 | 写经队列，读留 loop | **已入队** ✅ | 见 §4 语义确认 |

**判断依据总结**：判定标准 = **调用频率 × 所在线程 × 阻塞影响**。高阻塞（循环 execute_cypher）+ loop 线程 = 必入队；低频 + 已在 `to_thread` = 不入队。v5.24 唯一漏掉的两处都在 **dream 写路径**（API apply + scheduler auto_apply），v5.25 补齐。

---

## 3. 顺序依赖分析

| 文件 | 写调用序列 | 顺序要求 | 保证机制 |
|---|---|---|---|
| gateway_api `write_sensory` / `store_episode` / `store_multimodal` | create_episode → ensure_session → link_to_session | **有**（link 需 session+episode 存在） | 同 async 方法内 await → FIFO ✅ |
| dream `apply_candidate`（v5.25 新增入队） | PRUNE（逐 op）→ MERGE（读源 → SET → DETACH）→ `_mark_applied` | **严格**（先删后并，全部落库后标记） | **整体闭包入队**，写线程内串行 |
| dream_scheduler `auto_apply`（v5.25 移除） | PRUNE → `_persist_community_nodes`（三阶段边）→ 删 JSON | 严格（边操作三阶段） | **移除** → poll loop 整体闭包入队（同 `apply_candidate` 模式） |
| communities `reconcile` | resolve（内存）→ update_with_version | **有**（先检测后写） | resolve 留 loop，update_with_version 单 submit 原子 ✅ |
| ontology `batch_relations` | 每三元组：subj→obj→边（6 次 execute） | **有**（边依赖节点） | 闭包整体入队 ✅ |
| hebbian `_persist_batch` | 每连接：MATCH 边存在 → SET/INSERT | **有** | update 闭包整体入队 ✅ |
| `_persist_dream_state` | MATCH SystemNode → SET/INSERT | 有 | 闭包整体入队 ✅ |
| write.py `create_episode` 主链 | 主 episode → 各后续闭包 | 有 | v5.23 FIFO ✅ |

**新增顺序陷阱**：`apply_candidate` 入队后，`_mark_applied`（文件写）在闭包内**先于**闭包返回执行——意味着**写线程完成全部 GraphLite 写 + 标记 applied 后才返回**。若队列 503，则**既不写库也不标记**（`apply_candidate` 整体未执行）→ 幂等性保持（下次重试重新走全部流程）。这是"整体闭包入队"优于"拆开入队"的关键。

---

## 4. 风险：`qsubmit` 在非请求上下文（poll loop）的使用是否正确

**结论：v5.24 的实现基本正确，但 v5.25 需要确认/加固 3 处。**

### 4.1 `qsubmit` 设计正确性 — 已通过 v5.24 验证 ✅

`qsubmit`（`_deps.py:161`）是 async def，`q.submit` 内部 `asyncio.get_running_loop()`。poll loop（`_embed_poll_loop` / `_dream_poll_loop`）都是 async，直接 `await` 正确。**已验证**：`_process_embed_queue` 已改 async（`write.py:778`），`app.py:647` 已 `await`。

### 4.2 风险 A：`_persist_dream_state` 的 `create_task` 桥接 — 已正确，但兜底需加固（任务书 #3）

`app.py:55` 当前实现：
```python
loop = asyncio.get_running_loop()   # RuntimeError → loop=None
if loop is not None and loop.is_running():
    asyncio.create_task(_submit())
else:
    asyncio.run(_submit())
```
**正确性确认**：调度器回调（`_persist_state` 在 `check_and_trigger` / `trigger_explicit` / `_run_dream` finally）都在 loop 线程 → `create_task` 归属正确。`loop.is_running()` 防御 `RuntimeError` 正确。

**【v5.25 修复 #3】`_submit` 内只 catch `HTTPException`，SDK 异常（`execute_cypher` 抛 `ConnectionError` / `QueryError`）会漏入 fire-and-forget task** → "Task exception was never retrieved" 噪音。修复：
```python
async def _submit() -> None:
    try:
        await qsubmit(svc, _upsert_system_node, svc.graphlite_store,
                      "dream_scheduler_state", state_json)
    except HTTPException:
        logger.warning("Dream scheduler state persist deferred (write queue busy)")
    except Exception:
        # 【v5.25】SDK 异常兜底：非致命，只记日志不重抛（Task 异常噪音修复）
        logger.exception("Dream scheduler state persist failed (non-fatal)")
```

### 4.3 风险 B：poll loop 的 `await qsubmit` 阻塞 poll 周期 — 已接受 ✅

`_process_embed_queue` 内每条 hebbian `await qsubmit`，poll loop 等写完成（最多 wait_timeout=30s）。`await` 让出 loop（不阻塞事件循环），只延迟下一次 poll。**batch 大时串行排队**——已确认接受（判定 C，不合并）。v5.25 无改动。

### 4.4 风险 C：poll loop 触发的 503 语义 — 已正确 catch ✅

`write.py:816` / `app.py:604` 均已 `except HTTPException → logger.warning` 降级。503 不泄漏到 poll loop 外层。v5.25 确认无改动。

### 4.5 风险 D：A2A 独立进程的跨进程写-写并发 — 需记录，非 v5.25 阻断

`a2a_server.py:114` `_build_gateway` → `_init_services()` → **独立 Services + 独立 WriteQueue**。若 A2A 独立进程运行，与主 API 进程各自有写线程写同一 GraphLite → 跨进程写-写并发。**v5.25 不改**（非任务范围，且 A2A 生产启用与否未定），**记录到任务书结论**：若未来启用 A2A 独立进程，需改 `_build_gateway` 不建 WriteQueue（`svc.write_queue=None` → qsubmit 同步直调）或复用主进程。

### 4.6 风险 E（v5.25 新增）：`apply_candidate` 入队后 503 → 幂等性

整体闭包入队 → 503 时 `apply_candidate` 整体未执行（不写库不标记）→ 下次重试重走全部流程。**幂等保持**。catch HTTPException → 返回 "deferred" 消息（非 500）。

### 4.7 风险 F（v5.25 语义确认，任务书 #4）：search.py 队列忙降级无同步写

`search.py:194-200` 当前：
```python
if not deps.ontology_validator._ontology_synced:
    try:
        await qsubmit(deps, deps.ontology_validator.sync_entity_types_to_graphlite)
        await qsubmit(deps, deps.ontology_validator._populate_relationships)
        deps.ontology_validator._ontology_synced = True
    except HTTPException:
        logger.warning("Write queue busy, ontology sync deferred (validation continues)")
```
**确认语义**：队列忙（HTTPException）时 → `_ontology_synced` **保持 False** → 后续 `read_validate`（`ontology_validator.py:1156`）内部 `if not self._ontology_synced:` 会**在 loop 线程同步调 `sync_entity_types_to_graphlite` + `_populate_relationships`**（写！）——**这是 v5.24 遗留的"队列忙降级时首次检索仍可能同步写"**。

**【v5.25 修复 #4】两种方案**：
- **方案 A（推荐，零侵入）**：队列忙时**置 `_ontology_synced = True`**——跳过本次 lazy 同步，检索继续（纯读），写**延迟到下次**（下次非忙时 `read_validate` 不再触发同步，因为 flag 已置位 → **同步彻底丢失**）。❌ 不可行。
- **方案 B（推荐）**：catch 块内**保持 flag=False**（现状），但**确认无同步写残留**。❌ 现状有残留。
- **方案 C（正确修复）**：catch 块内**置 `_ontology_synced = True` + 把"延迟同步"交给写路径兜底**。但 `_ontology_synced` 置位后写路径 `extract_and_relate`（write.py:591）的 lazy sync 也会跳过 → **本体类型从未落库**。❌
- **方案 D（正确，推荐）**：**不依赖 `_ontology_synced` flag**——search.py 预同步失败时**允许 read_validate 内的同步写**，但**用 `asyncio.to_thread` 包住**（读路径本来就 to_thread 化，检索在 `asyncio.to_thread` 内执行，`read_validate` 的同步写在 to_thread 线程，不阻塞 loop）：
  ```python
  except HTTPException:
      # 【v5.25】队列忙 → 本次跳过预同步；read_validate 内 lazy 同步写
      # 在 to_thread 线程执行（不阻塞 loop），下次检索时预同步会重试
      logger.warning("Write queue busy, ontology sync deferred (validation continues)")
  ```
  **但**：检索 `results_raw` 已在 loop 线程（`search.py:57` `await asyncio.wait_for(asyncio.to_thread(...))` 已返回），`read_validate` 在 loop 线程调用（`search.py:201`）——**同步写在 loop**。❌

  真正正确：**read_validate 整体也包 `asyncio.to_thread`**（它是纯读 + 首次 lazy 写）。但 `read_validate` 返回结果用于覆盖 score，需 await。

**权衡后推荐方案 E（最小正确修复）**：队列忙时**置 `_ontology_synced = True` + 缓存一个 "pending sync" 标记，由写路径下次执行时补同步**。但这引入状态。**更简单**：队列忙时**保持 flag=False（现状），接受"极端超载瞬态下首次检索有一次同步写在 loop 线程"**——因为：
1. `sync_entity_types_to_graphlite` 首次执行一次（lazy），之后 flag=True 永不触发；
2. 触发条件 = 队列满（`max_pending=100`）且是**该进程首次检索**——两个条件同时满足概率极低；
3. 超载瞬态下 loop 线程卡一次（~1-2s）比"本体类型永不落库"危害小。

**最终结论**：**确认现状语义——队列忙时 `_ontology_synced` 保持 False，`read_validate` 首次 lazy 写回退 loop 线程同步执行。这是可接受的超载瞬态降级**（一次性、低频、写量小）。**记录到任务书**。若要求"绝对无同步写"，可改方案 D（read_validate 整体 to_thread），但那是行为变更 + 检索热路径改动，不建议 v5.25 做。

---

## 5. 实施顺序（给实现者）

按"最小侵入 → 验证"排序，**全部 5 项修复不碰 WriteQueue 核心**（`core/write_queue.py` / `_deps.py` / `a2a_server.py` diff 保持为 0，符合约束）：

1. **【修复 #3，最小侵入】`_persist_dream_state` SDK 异常兜底**（app.py:70-74）
   `_submit` 内加 `except Exception → logger.exception`。单文件、单闭包、零风险。

2. **【修复 #1，核心】dream.py:136 `apply_candidate` 整体入队**
   `await qsubmit(deps, store.apply_candidate, dream_id, deps.graphlite_store)` + catch HTTPException → "deferred" 503 语义。导入 `qsubmit`。

3. **【修复 dream auto_apply】dream_scheduler.py:321 移除 auto_apply**
   删除 `_run_dream` finally 段的 `auto_apply_candidates` 调用（含 try/except），保留 `_persist_state()`。auto_apply 完全由 app.py `_dream_poll_loop` 承担（已入队 + 队列深度守卫）。**顺序在 #2 后**（两处共用同一 `apply_candidate` 语义，先验证 API 路径）。

4. **【语义确认 #4】search.py 降级语义记录**
   不改代码，确认现状语义（§4.7 结论），在任务书补录结论。

5. **【评估 #2】hebbian 批量合并结论记录**
   不改代码，记录"不可合并，保持逐条"（判定 C）。

6. **版本 bump 5.24.0 → 5.25.0**：`VERSION` / `shm/_version.py`（`__version__` + `__version_name__` + changelog）/ `pyproject.toml` / `README.md` + `gh repo edit --description`（AGENTS.md 规范第 4 条）。

7. **测试**：新增 dream apply 入队测试（TestClient 路由 + 真实 WriteQueue + MagicMock store）、`_persist_dream_state` SDK 异常兜底测试（mock `_upsert_system_node` 抛异常 → 无 "Task exception never retrieved"）。全量 `pytest tests/ -q` 绿。

---

## 6. 风险汇总

| 风险 | 级别 | 处理 |
|---|---|---|
| `apply_candidate` 入队后 503 → 幂等性 | 中 | 整体闭包入队 → 503 时整体未执行 → 幂等保持 ✅ |
| `auto_apply` 移出 scheduler 后候选延迟 apply | 低 | 延迟 ≤1 poll interval，auto_apply 低频无感知 ✅ |
| search 队列忙降级首次检索同步写 | 低 | 确认可接受（一次性 lazy + 超载瞬态），记录结论 |
| A2A 独立进程跨进程写-写并发 | 中 | **记录到任务书结论**（v5.25 不改，未来 A2A 启用时处理） |
| hebbian 逐条 submit 大 batch 排队 | 低 | 记录"不可合并"（语义优先），常规负载可接受 |
| `_persist_dream_state` SDK 异常噪音 | 低 | 修复 #3 兜底 ✅ |

**一句话结论**：v5.25 = **dream API apply 入队（#1）+ scheduler auto_apply 移除转 poll loop（#2）+ `_persist_dream_state` SDK 异常兜底（#3）+ search 降级语义确认（#4）+ hebbian 不合并结论记录（#2 评估）**。队列核心零改动，请求路径写全部收敛；唯一残留风险（A2A 跨进程写-写并发）记录到任务书。