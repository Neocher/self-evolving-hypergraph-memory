工作树无未提交 diff——改动已提交。查看最近提交的 diff：
HEAD 即 v5.25 提交。审查完整 diff：
现在验证关键接线点：`qsubmit` 签名、`_dream_poll_loop` 是否真的承担 auto_apply、dream.py 完整上下文、测试文件：
WriteQueue 核心零改动确认（本提交仅 8 文件）。检查 scheduler 残留引用 + CI 版本断言 + search.py 语义现状：
CI 断言 `_version.py` == `pyproject.toml` == `VERSION` 三处一致；README 第四处已同步 5.25.0。检查 `_run_dream` 是否残留未用参数 + search.py 降级语义现状 + hebbian 逐条 submit 现状：
search.py 现状与任务书记录的降级语义一致；hebbian 仍逐条 submit（未合并）；scheduler 无 auto_apply 残留引用（`candidate_store` 仍传给 pipeline 用于梦境内持久化，非死代码）。运行测试验证：
483 passed 全绿（4 个警告均为既有无关警告：test_dream_ner_fix asyncio 警告 + sklearn 降维警告）。最后核对 `_persist_dream_state` 完整结构与 `_upsert_system_node` 签名（静默失败专项）：
## 审核结论

**审核范围**：HEAD `13be51e`（v5.25.0），8 文件改动。已对照 `.trio-task-v525.md` / `.trio-plan-v525.md` 逐一核验。

### 1. 接入正确性 ✅
- **dream.py:136**：`apply_candidate(dream_id, graphlite_store)` 整体闭包 `await qsubmit(deps, store.apply_candidate, ...)` 入队。`qsubmit(_deps.py:161)` 契约核验：队列满/关闭/超时 → `HTTPException(503)`；`apply_candidate`（dream_candidate_store.py:197-272）内部 try/except 每 op + 整体（永不抛 HTTPException）→ 路由 `except HTTPException` 只匹配 qsubmit 的 503，无误伤。参数位置序与签名 `(dream_id, graphlite_store)` 一致。PRUNE→MERGE→_mark_applied 顺序在写线程内串行保持；503 时整体未执行 → 幂等保持。✅
- **app.py `_persist_dream_state`**：`except Exception → logger.exception` 加在 `_submit()` 内，`create_task` 与 `asyncio.run` 两条路径都覆盖；`_upsert_system_node(store, node_id, payload)` 位置参数序正确；外层兜底保留。✅
- **scheduler auto_apply 移除**：`_dream_poll_loop`（app.py:590-612）实况确认已承担 auto_apply（qsubmit 整体闭包 + `pending_count() > max_pending//2` 队列深度守卫 + HTTPException/Exception 双兜底），功能未丢失，仅延迟 ≤1 poll interval。scheduler 内无 auto_apply 残留引用（仅注释），`candidate_store` 仍传给 pipeline（非死代码）。✅

### 2. hebbian 入队上下文 ✅
write.py:814 仍为逐条 `await qsubmit(deps, _run_hebbian_update, ...)`（未合并），与任务书"不可合并"结论一致——合并会改变 active_nodes 批内共激语义。poll loop 内 `await` 正确（`qsubmit` 是 async，poll loop 是 async 协程）。✅

### 3. WriteQueue 核心零改动 / 写路径无遗漏 ✅
提交仅含 8 文件（README/VERSION/pyproject/_version/app/dream/scheduler/测试）——`core/write_queue.py`、`_deps.py`、`a2a_server.py` 零 diff。任务书 4 项全覆盖：fix#1（API 入队）✅、fix#3（异常兜底）✅、fix#4（search 降级语义确认，search.py:194-200 现状与记录结论一致）✅、评估#2（记录"不可合并"）✅。

### 4. 版本四处同步 + 测试质量 ✅
四处均 5.25.0（CI 断言其中三处 + README 手动确认）；`__version_info__`/`__version_name__` 同步更新。测试 6 个走公共 HTTP 入口（TestClient + 真实 WriteQueue + 真实 `WriteQueueFullError`，非 mock 内置类），覆盖：入队执行、队列忙 deferred（断言 apply_candidate 未执行）、无队列直调降级、失败传播、SDK 异常兜底（loop + 无 loop 两路径）。**复核运行：`pytest tests/ -q` → 483 passed**，与提交声明一致（4 警告均为既有无关警告）。测试不碰真实记忆。✅

### 5. 静默失败专项 ✅
参数名/签名一致性核验通过（3 处 qsubmit 全部位置参数、序正确）；`except Exception` 可捕获 GraphLite SDK 自有 `ConnectionError/QueryError`（均继承 Exception）；无哨兵复用、无多语句 Cypher 新增、无熔断语义改动。

### 非阻断备注
1. 任务书对 scheduler auto_apply "已在 to_thread" 的假设**有误**（实为 `_run_dream` 协程内裸同步调用）；plan 以实况纠正并修复（移除→poll loop），属正确偏差。
2. 队列忙降级返回 **HTTP 200 + success=False**（而非字面 503 状态码）——与方案 §1.7 逐字一致，且与路由既有失败语义一致，可接受。
3. 防御性备注：`except HTTPException` 若未来 `apply_candidate` 自身抛 HTTPException 会被误标为 queue busy（当前不抛，风险仅存于未来改动）。
4. GitHub about 同步属发布动作，本次约束"不 push"未执行，留待发布时。

## 通过状态: 通过
问题清单（均非阻断）：① scheduler auto_apply 假设纠正偏差已记录；② 降级返回 200 非 503 状态码（与方案一致）；③ 泛化 HTTPException catch 的防御性备注；④ about 同步留待发布。