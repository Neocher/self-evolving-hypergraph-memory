## 设计 SHM 认证 + 速率限制系统

### 目标
为 SHM 全部 5 个协议入口添加认证和速率限制，为开源发布做准备。

### 设计要求 (Karpathy 风格)
1. **最小改动**: 不改 SHM Core (core/*, api/_routes.py)，只在网关层添加
2. **统一认证层**: 所有协议入口共享同一认证机制
3. **默认 DEV_MODE**: 开发环境无认证，生产环境强制
4. **零外部依赖**: 不用 Redis/JWT 库，用 token 文件 + Python 标准库

### 需要覆盖的协议入口
- HTTP REST (:8000) — FastAPI 中间件
- A2A HTTP (:8001) — FastAPI 中间件
- ACP HTTP (:8770) — FastAPI 中间件
- MCP stdio (:8002) — 启动时验证 token 文件权限
- CLI (终端) — 读取 token 文件

### 功能需求

#### 认证
- Token 存储在 `~/.shm/auth.tokens` (JSON, chmod 600)
- CLI 命令: `shm auth create-key <name>` → 生成新 token
- CLI 命令: `shm auth revoke-key <name>` → 删除 token
- 三个 HTTP 服务共享同一个 token 文件
- 无 token → 401, 错误 token → 403
- DEV_MODE=true 跳过认证

#### 速率限制
- 基于 IP 的内存速率限制（token bucket 算法）
- 默认: 1000 req/min, 10k writes/day
- 超限 → 429 + Retry-After
- 可配置: SHM_RATE_LIMIT env var
- 简单实现: 用 collections.defaultdict + time.time，不用 Redis

### 读取文件
1. /home/admin/shm/gateway/gateway_api.py — 理解网关结构
2. /home/admin/shm/gateway/mcp_server.py — MCP 入口
3. /home/admin/shm/gateway/a2a_server.py — A2A 入口
4. /home/admin/shm/gateway/cli.py — CLI 入口
5. /home/admin/shm/acp_bridge.py — ACP 入口
6. /home/admin/shm/api/app.py (create_app 函数) — FastAPI 入口

### 输出要求
1. 新增什么文件/类/方法
2. 每个协议入口的修改点（精确到行）
3. 代码量估算
4. 验证方法

读完文件后给出完整设计。
