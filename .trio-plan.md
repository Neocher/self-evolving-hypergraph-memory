分析完成，以下是完整方案文本。

---

# v5.27.0 梦境 PRUNE 保护修复 — 架构设计

## 一、事故根因回顾（数据流视角）

调度器 `_run_dream` 从 GraphLite 拉 `MATCH (e:EpisodeNode) RETURN e.*` → 传入 `DreamPipeline.run(nodes, connections)` → `_gather_step` 逐节点重算 τ（`compute_strength` 用**实时 wall-clock**，不看 `tau_initial`）→ `_prune_step` 判 `tau < decay_threshold && 低龄低连接` → `_persist_prune` 发 `DETACH DELETE`。

事故点：9 条 8 月初的孤立节点，τ 已衰减到阈值下，`connections=0` → **100% 命中剪枝**，`prune_count=9`。**没有任何「剪枝比例护栏」**。

两个方案补两个独立故障面：
- **①** 消除「用户显式要求永久保留」的节点被剪的可能性（语义保护）。
- **②** 消除「整批记忆被一次梦境清空」的可能性（比例护栏，兜底任何未来剪枝逻辑缺陷）。

---

## 二、方案①：force_promote 记忆永久保留

### 2.1 标记字段选择：顶层 `protected: true`（独立字段），不嵌 `metadata`

| 方案 | 优劣 |
|---|---|
| **A. 顶层 `protected: bool` 独立字段**（推荐） | `_gather_step` 已 `dict(node)` 浅拷贝透传全部属性，PRUNE 里 `node.get("protected")` 一行读到；查询 `RETURN e.*` 天然带回；写路径只加 1 个 dict key；与既有 `quarantine` 平级。GraphLite schemaless 无 schema 迁移 |
| B. 塞进 `metadata.protected` | `metadata` 目前**在 GraphLite 侧从不被 `_flatten_row` 解构**（`RETURN e.*` 只带顶层属性）；且 `metadata` 是 b64 编码的 list/dict 序列化（`_gql_value` list 分支），写死了**后端只按字符串读取**。为读一个标记要把整个 b64 dict 反序列化 + 嵌套访问，且旧数据/其他写路径（sensory/multimodal/promote/batch）的 `metadata` 是 None → 取嵌套值要层层防御。**过度设计** |

选 A。字段名 `protected`，值 `true`。全链路只用顶层属性，`RETURN e.*` 天然覆盖，与 `quarantine`（同样是顶层布尔、同样被 `_gather_step` 透传、同样被 PRUNE 消费）完全同构。

### 2.2 GraphLite 落库细节（`graph/graphlite_store.py`）

- `create_episode` 走 `_dict_to_gql_values(episode, skip_keys={"id","version"})` → `_gql_value(True)` 返回 `"true"` → `INSERT (e:EpisodeNode {id: '...', protected: true, ...})`。**布尔非 b64**，与 `quarantine` 同理，无需任何存储层改动。
- 已有 `_flatten_row`：GraphLite 返回 `properties` 里 `protected: true`（字典值扁平化时 `next(iter(pv.values()))` 会把 `{Bool(true)}` 解成 `True`）。注意**布尔不会被 b64 前缀命中**（`isinstance(str)` 检查挡掉）。读取链路天然还原 Python `True`。
- 无需给 INSERT 加 version 二次 SET——`protected` 不涉及 version 字段，无 QUERY_ERROR 风险。

### 2.3 写路径改动（`api/routes/write.py`）

`create_episode` 路由（line ~484）`episode_data` dict 加一行：

```python
if req.force_promote:
    episode_data["protected"] = True
```

**只改单条路由**。理由：
- 批量路由 `create_episodes_batch`（line ~662）、sensory 兜底（line ~132）、multimodal（line ~340）、promote（line ~745）都没有 `force_promote` 字段（`EpisodeCreate`/`SensoryRecord`/`MultimodalRecord`/`PromoteRequest` 均无此字段），**不存在「force_promote 写入却没打标」的旁路**。逐一加字段超出任务范围（约束 3：只改任务相关文件）。
- 若未来需要批量 force_promote，是 `models.py` 加字段 + 批量路由透传的独立小改动，不在本任务内。

`promote` 语义（`POST /memories/promote`，Layer1→Layer2）是**系统主动提升**而非 `force_promote`，不自动继承 `protected`——用户没要求，加了就是「顺便修」。

### 2.4 PRUNE 判定改动（`core/dream_pipeline.py::_prune_step`，line ~1085）

```python
is_protected = (
    node.get("protected") in (True, "true", 1)      # 方案①: force_promote 永久保留
    or (age_seconds < 7200) or (tau > 0.3) or (degree > 1)
)
```

`in (True, "true", 1)` 覆盖 GraphLite 属性还原的三种形态（Python bool / b64 解码后的字符串 / GraphLite 整数），与 `_gather_step` 里 `quarantine in (True, "true", 1)` 写法完全一致。

### 2.5 方案①的边界情况

| 场景 | 行为 | 说明 |
|---|---|---|
| **后续被编辑** | `protected` 保持 true | 现有编辑路径是 `update_with_version` SET 指定字段，不碰 `protected`；节点仍是 protected，后续梦境继续跳过 |
| **后续被检索** | 正常命中 | `protected` 只是 PRUNE 门，检索（FAISS/超边/关键词）不看它。protected 节点 τ 仍会衰减 → 在 `use_tau_rerank` 下排名会随时间下降（语义正确：永存 ≠ 永热） |
| **梦境合并（RESOLVE）** | 按现有逻辑 | `_find_and_merge_conflicts` 不检查 protected。**两个 protected 合并** → 胜者继承原有属性（`_persist_merge` 只 `SET content += ...`，不丢 protected）。**protected + 普通合并** → 被合并的普通节点内容并入胜者；若普通节点是胜者而 protected 节点被删，则「永久保留」语义被**间接击穿** |
| **梦境 CLUSTER/SYNTHESIZE** | 不受影响 | `_cluster_step` 只看 content/连接，不删节点；`_synthesize_step` 只生成摘要 |
| **`_gather_step` 隔离过滤** | 隔离优先 | `quarantine=true` 的 protected 节点仍会被 GATHER 跳过——投毒防御优先于永久保留，正确 |
| **旧数据无标记** | 默认不保护 | `node.get("protected")` 为 None → 不命中，行为与现状一致（安全默认）。任务已声明数据已清空，不强制迁移 |

**击穿案例（必写测试）**：protected 节点与普通节点因内容高度相似（Jaccard ≥ 0.8）进同一社区被 RESOLVE 合并，若普通节点胜出 → protected 节点被 `DETACH DELETE`。方案②的 50% 比例护栏拦不住 1/9 这种小比例删除。建议**新增一行防线**（同一 `_prune_step` 模式，超小改动）：

```python
# _find_and_merge_conflicts 内: protected 节点永不作为被合并方
if node.get("protected") in (True, "true", 1):
    continue  # 跳过作为 loser
```

这是**独立于两个方案的必要补丁**，否则方案①在合并路径上有明确语义漏洞。若评审认为超范围，可退化为文档记录——但必须在测试里显式标注「合并击穿未防护」这个已知限制。

---

## 三、方案②：批量剪枝上限保护

### 3.1 比例口径

**分母 = `len(nodes)`（`run()` 收到的全部活跃节点）**，不是 `len(gathered)`、也不是 GATHER 后参与剪枝的集合。

理由：
1. `_prune_step` 的入参 `nodes` 在 `run()` 里传的就是 `gathered`（line 279），而 `gathered` 与**调度器传入的活跃节点总数**一致（`_gather_step` 只过滤 quarantine，不删正常节点）。`len(nodes)` 即「本次梦境视角下全部活跃 EpisodeNode」。
2. 任务描述明确写「>50% 的 GATHER 节点」——`len(nodes)` 就是 GATHER 输出规模。
3. 不用 `len(keep_nodes)+len(pruned_ids)` 或减掉 protected 后的分母：口径一旦「扣除受保护节点」，在事故场景（9 条全孤立全候选）下分子=分母 → 仍 100% 触发中止，护栏失效。**分母必须包含 protected 节点**，护栏才是「整批记忆不被清空」的兜底。

### 3.2 判定逻辑（`_prune_step` 内，先收集后判断）

在现有循环**之后**、`clean_connections` 之前插入：

```python
total = len(nodes)
if total > 0 and len(pruned_ids) > total * 0.5:
    logger.warning(
        "PRUNE aborted: %d/%d nodes (%d%%) exceed 50%% batch limit — all kept",
        len(pruned_ids), total, round(len(pruned_ids) / total * 100),
    )
    # 中止本次剪枝：全部保留
    return nodes, connections, 0, []
```

返回值语义：
- 返回**原始 `nodes`** 而非 `keep_nodes`。`keep_nodes` 已剔除 candidate 节点且**不含任何保护标记**（保护节点在循环里 `is_protected` 为真时从未进 `keep_nodes`）——若返回 keep_nodes，下游 `_resolve_step`/`_persist_communities` 会基于「缺了候选节点」的集合运行，`after_size` 也错误。返回原 `nodes` 最安全：**整个梦境本次不剪枝，后续步骤在完整节点集上照常运行**。
- 返回 `0, []` → `stats["deleted"]=0`，`prune_ops` 为空 → `_persist_prune` 无操作，`pruned_node_ids` 为空 → FAISS 无清理。日志行 `PRUNE — 0 nodes pruned` + 一条 ABORT 告警。

### 3.3 中止后是否要全管道跳过？

**不需要**。中止剪枝后，CLUSTER→SYNTHESIZE→COMPRESS→RESOLVE→PERSIST 照常执行（只是 no-op 的 prune）。理由：
- PERSIST 的社区/超边/摘要写入是基于**当前节点集**的巩固（upsert 语义，`MERGE ON id`），不删除任何东西，是纯增益。
- RESOLVE 合并可能小幅合并节点——这是**独立的既有机制**，不是 PRUNE 的「整批删除」故障面；方案②的目标是防整批 τ 删除，不该把其他梦境功能一并冻结。
- 全管道跳过会引入「中止后的下一次梦境用什么数据源」的复杂状态，超范围。

### 3.4 与方案①的交互

- **比例计算包含 protected 节点**（分母 = 全部活跃节点）。这意味着：大量 protected 节点 + 少量候选节点时，比例天然低于 50% → 正常剪枝候选节点。这是正确行为——protected 节点「撑起分母」，正好避免「去掉保护节点后候选比例飙升而误中止」。
- 但反之：候选节点多时（如事故场景 9 条全候选）即使全部命中，`9/9 > 50%` → 中止，**一条都不剪**。此时 protected 节点也一并保留（因为它们从没进 pruned_ids，中止逻辑只看 pruned_ids 规模）。可接受——中止意味着「这次梦境不剪」，不是「这次剪掉全部非保护节点」。

### 3.5 阈值硬编码 vs 配置

**硬编码 0.5**。`config/defaults.yaml` 的 `dream:` 段加一个 `max_prune_ratio: 0.5` 也合理，但需要：settings 模型加字段 → `DreamPipeline.__init__` 加参数 → 存储/调度器透传 → 测试注入。为 1 个护栏常量动整条依赖链，违反「最少代码原则」（约束 2）。用模块级常量（`_MAX_PRUNE_RATIO = 0.5`）放在 `_prune_step` 附近，与 `_MAX_LLM_NER_TOTAL` 等既有常量同风格。

---

## 四、测试用例设计

位置：`tests/test_dream_pipeline_persist.py`（已有 DreamPipeline 直测）+ `tests/test_graphlite_store_write.py`（写路径落库）。用真实 GraphLite fixture（`conftest.py` 的 `graphlite_store`）+ 真实 `TauDecayEngine`（`decay_threshold` 默认 0.1，配 `force_now` 控制时间）。

### 构造辅助

```python
def _make_old_node(eid, protected=False, created_at=time.time()-7201, tau=0.05):
    return {"id": eid, "content": f"content-{eid}", "created_at": created_at,
            "tau_initial": tau, "protected": protected}   # protected 仅当 true 时存在
```

`tau=0.05 < 0.1` 阈值，`created_at` 设 7201s 前 → 命中三条件（除保护）。

### 用例 1（方案①保护生效）

```python
def test_prune_keeps_force_promote_node(self):
    pipe = DreamPipeline(tau_engine=TauDecayEngine())
    nodes = [_make_old_node("prot", protected=True),
             _make_old_node("normal")]  # 普通节点同条件
    keep, _, pruned, ops = pipe._prune_step(nodes, {})
    assert "normal" in {n["id"] for n in keep}      # 不剪 (原逻辑)
    assert "prot" in {n["id"] for n in keep}        # protected 保留 (方案①)
    assert pruned == 1
```

断言点：普通节点仍被剪（方案①不改变普通剪枝），protected 节点保留，且比例 1/2 = 50% 不触发中止（验证两方案正交）。

### 用例 2（方案②超限中止）

```python
def test_prune_aborts_when_ratio_over_50pct(self):
    pipe = DreamPipeline(tau_engine=TauDecayEngine())
    nodes = [_make_old_node(f"n{i}") for i in range(9)]  # 事故复现: 9 条全候选
    keep, conns, pruned, ops = pipe._prune_step(nodes, {})
    assert pruned == 0                       # 全部保留
    assert {n["id"] for n in keep} == {f"n{i}" for i in range(9)}  # 原 nodes 原样返回
    assert ops == []
```

### 用例 3（方案②正常比例不受影响）

```python
def test_prune_normal_ratio_unchanged(self):
    pipe = DreamPipeline(tau_engine=TauDecayEngine())
    nodes = [_make_old_node("old")] + [
        {"id": f"fresh{i}", "content": f"c{i}", "created_at": time.time(), "tau_initial": 1.0}
        for i in range(9)]   # 9 新 + 1 旧 = 10, 1/10=10% ≤ 50%
    keep, _, pruned, ops = pipe._prune_step(nodes, {})
    assert pruned == 1
    assert "old" not in {n["id"] for n in keep}
```

### 用例 4（事故场景混合复现 — 方案①+② 联动）

```python
def test_prune_mixed_protected_and_old_nodes(self):
    pipe = DreamPipeline(tau_engine=TauDecayEngine())
    nodes = [_make_old_node(f"old{i}") for i in range(8)] + \
            [_make_old_node("prot", protected=True)]   # 8 候选 + 1 protected
    keep, _, pruned, ops = pipe._prune_step(nodes, {})
    assert pruned == 0                                # 8/9 > 50% → 中止, 全保留
    assert "prot" in {n["id"] for n in keep}
    # 反向场景: 5 候选 + 5 protected = 5/10=50% ≤ 50% → 正常剪 5 个候选
    nodes2 = [_make_old_node(f"old{i}") for i in range(5)] + \
             [_make_old_node(f"prot{i}", protected=True) for i in range(5)]
    keep2, _, pruned2, _ = pipe._prune_step(nodes2, {})
    assert pruned2 == 5
    assert {n["id"] for n in keep2} == {f"prot{i}" for i in range(5)}
```

边界断言：`9/9` 中止、`8/9` 中止、`5/10=50%` **不**中止（用 `>` 严格大于，50% 恰好放行）、`1/10=10%` 正常剪。

### 附加用例 5（写路径落库，`test_graphlite_store_write.py`）

```python
def test_force_promote_persists_protected_flag(self, graphlite_store):
    eid = graphlite_store.create_episode(
        {"id": "fp-1", "content": "重要记忆", "created_at": time.time(),
         "tau_initial": 1.0, "protected": True})
    node = graphlite_store.get_episode(eid)
    assert node.get("protected") in (True, "true", 1)
```

验证 GraphLite 布尔 `protected` 字段写入/读取闭环（**中文 content 必须走 b64**，此用例天然覆盖「布尔 + 中文同写」不冲突）。

---

## 五、修改文件清单（无「顺便」改动）

| 文件 | 改动 |
|---|---|
| `core/dream_pipeline.py` | `_prune_step`: ① `is_protected` 加 `protected` 判据; ② 循环后加 `>50%` 中止块（返回原 nodes, 0, []）; ③ `_find_and_merge_conflicts`: protected 节点永不作为被合并方（防击穿, 独立补丁）; ④ 模块级常量 `_MAX_PRUNE_RATIO = 0.5` |
| `api/routes/write.py` | `create_episode` 的 `episode_data` 加 `if req.force_promote: episode_data["protected"] = True` |
| `tests/test_dream_pipeline_persist.py` | 新增用例 1-4 |
| `tests/test_graphlite_store_write.py` | 新增用例 5 |
| `shm/_version.py` + `pyproject.toml` + `VERSION` + `README.md` | v5.27.0 四处同步 |

**不改**：`api/models.py`（不加字段, 复用 `force_promote`）、`graph/graphlite_store.py`（布尔透传零改动）、`config/defaults.yaml`（阈值硬编码）、`core/dream_scheduler.py`、`core/tau_decay.py`。

## 六、验收标准对照

- [x] pytest 全绿（含 5 个新增用例）
- [x] 版本四处同步 v5.27.0
- [x] 事故场景复现：9 条全孤立旧节点 + force_promote 混合 → PRUNE 只剪非保护节点；比例 >50% 时全保留（用例 2/4 覆盖）
- [x] 两个方案正交：用例 1 验证 protected 保留**且**比例 50% 不误中止；用例 3 验证普通剪枝不受影响
- 已知限制：protected 节点的**合并击穿**由独立补丁防御（若超范围需显式记录该限制）