Now the version sync and test quality:
17 tests pass. Now verifying the full suite and the GraphLite index syntax claim (the connect test uses a FakeSession that only records calls — it doesn't prove real GraphLite parses the DDL):